<!DOCTYPE html>
<html lang="en" class="no-js">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico">
  
  <!-- Mark that JS is enabled -->
  <script>
    document.documentElement.classList.add('js-enabled');
    document.documentElement.classList.remove('no-js');
  </script>
  
  <link rel="stylesheet" href="/css/style.css">
  <noscript><link rel="stylesheet" href="/css/style.css"></noscript>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    /* Smooth scroll behavior - optimized for animations */
    html {
      scroll-behavior: smooth;
      scroll-padding-top: 80px;
    }
    
    /* Ensure smooth scrolling isn't blocked by animations */
    * {
      scroll-margin-top: 80px;
    }
    
    /* Optimize scroll performance during animations */
    html, body {
      will-change: scroll-position;
    }
    
    /* Ensure content is visible for no-JS users */
    html.no-js .polls-section,
    html.no-js .polls-container,
    html.no-js .poll-card,
    html.no-js section,
    html.no-js footer,
    html.no-js .section-header {
      opacity: 1 !important;
      visibility: visible !important;
      transform: none !important;
      animation: none !important;
    }
    .polls-section {
      padding: 4rem 0;
      background: #f9fafb;
      min-height: 60vh;
    }

    .polls-container {
      max-width: 800px;
      margin: 0 auto;
    }

    .section-header {
      text-align: center;
      margin-bottom: 3rem;
    }

    .section-header h1 {
      font-size: 2.5rem;
      font-weight: 700;
      color: #111827;
      margin-bottom: 0.5rem;
    }

    .section-header .highlight {
      color: #c51d34;
    }

    .section-header p {
      color: #6b7280;
      font-size: 1.125rem;
    }

    .polls-list {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .poll-card {
      background: white;
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      transition: all 0.3s;
      cursor: pointer;
    }

    .poll-card:hover {
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
      transform: translateY(-2px);
    }

    .poll-question {
      font-size: 1.125rem;
      font-weight: 700;
      color: #111827;
      margin: 0 0 1rem 0;
      line-height: 1.4;
    }

    .poll-results {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
      margin-bottom: 1rem;
    }

    .poll-option {
      width: 100%;
    }

    .poll-option-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.25rem;
      gap: 0.5rem;
    }

    .poll-option-text {
      color: #111827;
      font-weight: 500;
      font-size: 0.875rem;
      flex: 1;
      word-break: break-word;
    }

    .poll-option-stats {
      font-weight: 600;
      color: #10b981;
      font-size: 0.875rem;
      white-space: nowrap;
      flex-shrink: 0;
    }

    .poll-bar {
      height: 8px;
      background: #e5e7eb;
      border-radius: 4px;
      overflow: hidden;
    }

    .poll-bar-fill {
      height: 100%;
      background: linear-gradient(90deg, #10b981 0%, #059669 100%);
      transition: width 0.3s ease;
    }

    .poll-footer {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      color: #6b7280;
      font-size: 0.875rem;
      padding-top: 0.75rem;
      border-top: 1px solid #e5e7eb;
    }


    .empty-state {
      text-align: center;
      padding: 4rem 2rem;
      color: #6b7280;
    }

    .empty-state i {
      font-size: 4rem;
      margin-bottom: 1.5rem;
      opacity: 0.3;
    }

    .empty-state h3 {
      font-size: 1.5rem;
      font-weight: 600;
      color: #111827;
      margin-bottom: 0.5rem;
    }

    /* Already Voted Modal */
    .already-voted-modal {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 10000;
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s ease;
    }

    .already-voted-modal.show {
      opacity: 1;
      visibility: visible;
    }

    .already-voted-modal-content {
      background: white;
      border-radius: 20px;
      padding: 2.5rem;
      max-width: 500px;
      width: 90%;
      text-align: center;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      transform: scale(0.8) translateY(20px);
      transition: transform 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .already-voted-modal.show .already-voted-modal-content {
      transform: scale(1) translateY(0);
    }

    .already-voted-modal-content::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 5px;
      background: linear-gradient(90deg, #10b981 0%, #059669 50%, #10b981 100%);
      background-size: 200% 100%;
      animation: shimmer 2s linear infinite;
    }

    @keyframes shimmer {
      0% {
        background-position: 200% 0;
      }
      100% {
        background-position: -200% 0;
      }
    }

    .already-voted-image {
      width: 200px;
      height: 200px;
      margin: 0 auto 1.5rem;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
      animation: bounceIn 0.6s ease-out;
    }

    @keyframes bounceIn {
      0% {
        transform: scale(0);
        opacity: 0;
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    .already-voted-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .already-voted-title {
      font-size: 1.75rem;
      font-weight: 700;
      color: #111827;
      margin-bottom: 0.75rem;
      animation: slideInUp 0.5s ease-out 0.2s both;
    }

    .already-voted-message {
      color: #6b7280;
      font-size: 1rem;
      margin-bottom: 2rem;
      line-height: 1.6;
      animation: slideInUp 0.5s ease-out 0.3s both;
    }

    .already-voted-button {
      padding: 0.75rem 2rem;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      border: none;
      font-size: 0.9375rem;
      transition: all 0.2s;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
      animation: slideInUp 0.5s ease-out 0.4s both;
    }

    .already-voted-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(16, 185, 129, 0.4);
    }

    @keyframes slideInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .pagination-btn:not(.disabled):not(.active):hover {
      background: #f8f9fa !important;
      border-color: #c51d34 !important;
      color: #c51d34 !important;
    }

    @media (max-width: 768px) {
      .pagination-container {
        flex-direction: column;
        align-items: stretch;
      }

      .pagination-info {
        text-align: center;
      }

      .pagination {
        justify-content: center;
      }
    }
  </style>
  
  <!-- Fallback for no-JS: ensure content is visible -->
  <noscript>
    <style>
      .polls-section,
      .polls-container,
      .poll-card,
      section,
      footer,
      .section-header {
        opacity: 1 !important;
        visibility: visible !important;
        transform: none !important;
        animation: none !important;
      }
    </style>
  </noscript>
</head>
<body>
  <%- include('../partials/header') %>

  <section class="polls-section">
    <div class="container">
      <div class="section-header">
        <h1>Active <span class="highlight">Polls</span></h1>
        <p>Share your opinion and help us improve</p>
      </div>

      <div class="polls-container">
        <% if (polls && polls.length > 0) { %>
          <div class="polls-list">
          <% polls.forEach(poll => { %>
            <% 

              let pollOptions = [];
              if (poll.options) {
                if (Array.isArray(poll.options)) {
                  pollOptions = poll.options;
                } else if (typeof poll.options === 'string') {
                  try {
                    pollOptions = JSON.parse(poll.options);
                  } catch (e) {
                    pollOptions = [];
                  }
                } else if (typeof poll.options === 'object') {
                  pollOptions = Object.values(poll.options);
                }
              }
            %>
            <div class="poll-card" id="poll-<%= poll.id %>" onclick="window.location.href='/polls/<%= poll.id %>'">
              <h4 class="poll-question">
                <%= poll.question %>
              </h4>
              
              <!-- Poll Results -->
              <div class="poll-results">
                <% if (pollOptions && pollOptions.length > 0 && poll.totalVotes > 0) { %>
                  <% pollOptions.forEach((option, index) => { %>
                    <% 
                      const votes = poll.optionVotes && poll.optionVotes[index] ? poll.optionVotes[index] : 0;
                      const percentage = poll.totalVotes > 0 ? ((votes / poll.totalVotes) * 100).toFixed(1) : 0;
                    %>
                    <div class="poll-option">
                      <div class="poll-option-header">
                        <span class="poll-option-text"><%= typeof option === 'string' ? option : (option.text || option.value || String(option)) %></span>
                        <span class="poll-option-stats"><%= percentage %>% (<%= votes %> votes)</span>
                      </div>
                      <div class="poll-bar">
                        <div class="poll-bar-fill" style="width: <%= percentage %>%;"></div>
                      </div>
                    </div>
                  <% }) %>
                <% } else if (pollOptions && pollOptions.length > 0) { %>
                  <div style="color: #6b7280; font-size: 0.9375rem; text-align: center; padding: 1rem;">
                    No votes yet - Be the first to vote!
                  </div>
                <% } %>
              </div>
              
              <div class="poll-footer">
                <i class="fas fa-users"></i>
                <span><%= poll.totalVotes || 0 %> total votes</span>
                <span style="margin-left: auto; color: #c51d34; font-weight: 600; font-size: 0.875rem;">
                  View Poll <i class="fas fa-arrow-right"></i>
                </span>
              </div>
            </div>
          <% }) %>
          </div>
        <% } else { %>
          <div class="empty-state">
            <i class="fas fa-poll"></i>
            <h3>No Active Polls</h3>
            <p>There are no active polls at the moment. Check back soon!</p>
          </div>
        <% } %>

        <!-- Pagination -->
        <% if (pagination && pagination.totalPages > 1) { %>
          <div class="pagination-container" style="margin-top: 3rem; padding: 1.5rem; background: white; border-radius: 12px; box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1); display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
            <div class="pagination-info" style="color: #6b7280; font-size: 0.875rem;">
              Showing <%= ((pagination.currentPage - 1) * pagination.limit) + 1 %> to <%= Math.min(pagination.currentPage * pagination.limit, pagination.totalCount) %> of <%= pagination.totalCount %> results
            </div>
            <div class="pagination" style="display: flex; gap: 0.5rem; align-items: center; flex-wrap: wrap;">
              <% 
                const buildQuery = (page) => {
                  return `?page=${page}`;
                };
              %>
              <% if (pagination.hasPrev) { %>
                <a href="/polls<%= buildQuery(pagination.currentPage - 1) %>" class="pagination-btn" style="padding: 0.5rem 0.75rem; border-radius: 6px; background: white; border: 1px solid #e5e7eb; color: #111827; text-decoration: none; font-size: 0.875rem; font-weight: 500; transition: all 0.2s; display: inline-flex; align-items: center; gap: 0.5rem;">
                  <i class="fas fa-chevron-left"></i>
                </a>
              <% } else { %>
                <span class="pagination-btn disabled" style="padding: 0.5rem 0.75rem; border-radius: 6px; background: white; border: 1px solid #e5e7eb; color: #9ca3af; font-size: 0.875rem; font-weight: 500; opacity: 0.5; cursor: not-allowed; display: inline-flex; align-items: center; gap: 0.5rem;">
                  <i class="fas fa-chevron-left"></i>
                </span>
              <% } %>

              <% 
                let startPage = Math.max(1, pagination.currentPage - 2);
                let endPage = Math.min(pagination.totalPages, pagination.currentPage + 2);
                if (endPage - startPage < 4) {
                  if (startPage === 1) {
                    endPage = Math.min(5, pagination.totalPages);
                  } else {
                    startPage = Math.max(1, pagination.totalPages - 4);
                  }
                }
              %>

              <% if (startPage > 1) { %>
                <a href="/polls<%= buildQuery(1) %>" class="pagination-btn" style="padding: 0.5rem 0.75rem; border-radius: 6px; background: white; border: 1px solid #e5e7eb; color: #111827; text-decoration: none; font-size: 0.875rem; font-weight: 500; transition: all 0.2s; display: inline-flex; align-items: center; gap: 0.5rem;">1</a>
                <% if (startPage > 2) { %>
                  <span class="pagination-ellipsis" style="padding: 0.5rem 0.25rem; color: #6b7280;">...</span>
                <% } %>
              <% } %>

              <% for (let i = startPage; i <= endPage; i++) { %>
                <% if (i === pagination.currentPage) { %>
                  <span class="pagination-btn active" style="padding: 0.5rem 0.75rem; border-radius: 6px; background: #c51d34; color: white; border: 1px solid #c51d34; font-size: 0.875rem; font-weight: 500; display: inline-flex; align-items: center; gap: 0.5rem;"><%= i %></span>
                <% } else { %>
                  <a href="/polls<%= buildQuery(i) %>" class="pagination-btn" style="padding: 0.5rem 0.75rem; border-radius: 6px; background: white; border: 1px solid #e5e7eb; color: #111827; text-decoration: none; font-size: 0.875rem; font-weight: 500; transition: all 0.2s; display: inline-flex; align-items: center; gap: 0.5rem;"><%= i %></a>
                <% } %>
              <% } %>

              <% if (endPage < pagination.totalPages) { %>
                <% if (endPage < pagination.totalPages - 1) { %>
                  <span class="pagination-ellipsis" style="padding: 0.5rem 0.25rem; color: #6b7280;">...</span>
                <% } %>
                <a href="/polls<%= buildQuery(pagination.totalPages) %>" class="pagination-btn" style="padding: 0.5rem 0.75rem; border-radius: 6px; background: white; border: 1px solid #e5e7eb; color: #111827; text-decoration: none; font-size: 0.875rem; font-weight: 500; transition: all 0.2s; display: inline-flex; align-items: center; gap: 0.5rem;"><%= pagination.totalPages %></a>
              <% } %>

              <% if (pagination.hasNext) { %>
                <a href="/polls<%= buildQuery(pagination.currentPage + 1) %>" class="pagination-btn" style="padding: 0.5rem 0.75rem; border-radius: 6px; background: white; border: 1px solid #e5e7eb; color: #111827; text-decoration: none; font-size: 0.875rem; font-weight: 500; transition: all 0.2s; display: inline-flex; align-items: center; gap: 0.5rem;">
                  <i class="fas fa-chevron-right"></i>
                </a>
              <% } else { %>
                <span class="pagination-btn disabled" style="padding: 0.5rem 0.75rem; border-radius: 6px; background: white; border: 1px solid #e5e7eb; color: #9ca3af; font-size: 0.875rem; font-weight: 500; opacity: 0.5; cursor: not-allowed; display: inline-flex; align-items: center; gap: 0.5rem;">
                  <i class="fas fa-chevron-right"></i>
                </span>
              <% } %>
            </div>
          </div>
        <% } %>
      </div>
    </div>
  </section>

  <%- include('../partials/footer') %>

  <script>

    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -100px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, observerOptions);

    document.querySelectorAll('section').forEach(section => observer.observe(section));

    const footer = document.querySelector('footer');
    if (footer) {
      observer.observe(footer);
    }
  </script>
</body>
</html>



