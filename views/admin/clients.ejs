<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      margin: 0;
      padding: 0;
      margin-top: 0;
      padding-top: 0;
    }

    :root {
      --bg-primary: #ffffff;
      --bg-secondary: #f9fafb;
      --border-primary: #e5e7eb;
      --text-primary: #111827;
      --text-secondary: #6b7280;
      --accent-red: #c51d34;
    }

    .dashboard-layout {
      display: flex;
      min-height: 100vh;
      margin: 0;
      padding: 0;
      margin-top: 0;
      padding-top: 0;
    }

    .dashboard-main {
      flex: 1;
      padding: 0.25rem 2.5rem 2rem 2.5rem;
      margin-left: 280px;
      background: var(--bg-secondary);
      width: calc(100% - 280px);
      min-width: 0;
      margin-top: 0;
      padding-top: 0.25rem;
    }

    .page-header {
      margin-bottom: 2rem;
      margin-top: 0;
      padding-top: 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .page-header h1 {
      font-size: 2rem;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 0.5rem;
      line-height: 1.2;
    }

    .page-header p {
      color: var(--text-secondary);
      font-size: 0.9375rem;
      margin: 0;
    }

    .header-actions {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .filters-section {
      background: var(--bg-primary);
      border-radius: 12px;
      box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.05);
      padding: 1.5rem;
      margin-bottom: 1.5rem;
      overflow: hidden;
    }

    .filters-grid {
      display: grid;
      grid-template-columns: 1fr 1fr auto;
      gap: 1rem;
      align-items: end;
    }

    .filter-group {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .filter-group label {
      font-size: 0.875rem;
      font-weight: 500;
      color: var(--text-secondary);
    }

    .filter-input {
      padding: 0.625rem 0.75rem;
      border-radius: 6px;
      border: 1px solid var(--border-primary);
      font-size: 0.875rem;
      background: white;
      transition: border-color 0.2s;
      width: 100%;
      box-sizing: border-box;
    }

    .filter-input:focus {
      outline: none;
      border-color: #c51d34;
      box-shadow: 0 0 0 3px rgba(197, 29, 52, 0.1);
    }

    .btn-filter {
      padding: 0.625rem 1.25rem;
      border-radius: 8px;
      font-size: 0.875rem;
      font-weight: 500;
      cursor: pointer;
      border: none;
      transition: all 0.2s;
      background: var(--accent-red);
      color: white;
      touch-action: manipulation;
      -webkit-tap-highlight-color: transparent;
      white-space: nowrap;
    }

    .btn-filter:hover {
      background: #a01628;
    }

    .btn {
      padding: 0.5rem 1rem;
      border-radius: 6px;
      font-size: 0.875rem;
      font-weight: 500;
      cursor: pointer;
      border: none;
      transition: all 0.2s;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      touch-action: manipulation;
      -webkit-tap-highlight-color: transparent;
    }

    .btn-sm {
      padding: 0.5rem;
      font-size: 0.875rem;
      width: 36px;
      height: 36px;
      justify-content: center;
    }

    .btn-icon-only {
      padding: 0.5rem;
      width: 36px;
      height: 36px;
      gap: 0;
    }

    .btn-primary {
      background: var(--accent-red);
      color: white;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      border: none;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      white-space: nowrap;
    }

    .btn-primary:hover {
      background: #a01628;
    }

    .btn-danger {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
    }

    .btn-danger:hover {
      background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(239, 68, 68, 0.4);
    }

    .btn-secondary {
      background: linear-gradient(135deg, #6b7280 0%, #4b5563 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(107, 114, 128, 0.3);
    }

    .btn-secondary:hover {
      background: linear-gradient(135deg, #4b5563 0%, #374151 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(107, 114, 128, 0.4);
    }

    .btn-info {
      background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
    }

    .btn-info:hover {
      background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(59, 130, 246, 0.4);
    }

    .table-container {
      background: var(--bg-primary);
      border-radius: 12px;
      box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.05);
      overflow: hidden;
      width: 100%;
      max-width: 100%;
    }

    .table {
      width: 100%;
      border-collapse: collapse;
      table-layout: auto;
    }

    .table-wrapper {
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
    }

    .table thead {
      background: var(--bg-secondary);
    }

    .table th {
      padding: 1rem 1.5rem;
      text-align: left;
      font-weight: 600;
      font-size: 0.875rem;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .table td {
      padding: 1rem 1.5rem;
      border-top: 1px solid var(--border-primary);
      color: var(--text-primary);
    }

    .table tbody tr:hover {
      background: var(--bg-secondary);
    }

    .client-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      object-fit: cover;
    }

    .action-buttons {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 0.5rem;
      max-width: 80px;
    }

    .action-buttons .btn {
      min-width: 36px;
      min-height: 36px;
      touch-action: manipulation;
    }

    .empty-state {
      text-align: center;
      padding: 3rem;
      color: var(--text-secondary);
    }

    .empty-state i {
      font-size: 3rem;
      margin-bottom: 1rem;
      opacity: 0.3;
    }

    .pagination-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 1.5rem;
      padding: 1rem 0;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .pagination-info {
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    .pagination {
      display: flex;
      gap: 0.5rem;
      align-items: center;
      flex-wrap: wrap;
    }

    .pagination-btn {
      padding: 0.5rem 0.75rem;
      border-radius: 6px;
      background: var(--bg-primary);
      border: 1px solid var(--border-primary);
      color: var(--text-primary);
      text-decoration: none;
      font-size: 0.875rem;
      font-weight: 500;
      transition: all 0.2s;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      cursor: pointer;
    }

    .pagination-btn:hover:not(.disabled):not(.active) {
      background: var(--bg-secondary);
      border-color: var(--accent-red);
      color: var(--accent-red);
    }

    .pagination-btn.active {
      background: var(--accent-red);
      color: white;
      border-color: var(--accent-red);
    }

    .pagination-btn.disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

    .pagination-ellipsis {
      padding: 0.5rem 0.25rem;
      color: var(--text-secondary);
    }

    @media (max-width: 768px) {
      .pagination-container {
        flex-direction: column;
        align-items: stretch;
      }

      .pagination-info {
        text-align: center;
      }

      .pagination {
        justify-content: center;
      }
    }

    /* Mobile Card Layout */
    .mobile-card-view {
      display: none;
      padding: 0.5rem;
    }

    .mobile-card {
      background: var(--bg-primary);
      border: 1px solid var(--border-primary);
      border-radius: 12px;
      padding: 1rem;
      margin-bottom: 0.75rem;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
      transition: all 0.2s ease;
    }

    .mobile-card:hover {
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
    }

    .mobile-card-header {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      margin-bottom: 0.75rem;
      padding-bottom: 0.75rem;
      border-bottom: 1px solid var(--border-primary);
    }

    .mobile-card-avatar {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      object-fit: cover;
      flex-shrink: 0;
    }

    .mobile-card-title {
      flex: 1;
      min-width: 0;
    }

    .mobile-card-title h3 {
      margin: 0 0 0.25rem 0;
      font-size: 0.9375rem;
      font-weight: 700;
      color: var(--text-primary);
      overflow: hidden;
      text-overflow: ellipsis;
      display: -webkit-box;
      -webkit-line-clamp: 1;
      -webkit-box-orient: vertical;
      word-break: break-word;
    }

    .mobile-card-title p {
      margin: 0;
      font-size: 0.8125rem;
      color: var(--text-secondary);
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .mobile-card-body {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .mobile-card-row {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 0.75rem;
      font-size: 0.8125rem;
    }

    .mobile-card-label {
      color: var(--text-secondary);
      font-weight: 600;
      min-width: 60px;
      flex-shrink: 0;
      font-size: 0.8125rem;
    }

    .mobile-card-value {
      color: var(--text-primary);
      text-align: right;
      flex: 1;
      word-break: break-word;
      font-size: 0.8125rem;
    }

    .mobile-card-brand {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      justify-content: flex-end;
      min-width: 0;
      flex: 1;
    }

    .mobile-card-brand-logo {
      width: 24px;
      height: 24px;
      object-fit: contain;
      flex-shrink: 0;
    }

    .mobile-card-stats {
      display: flex;
      gap: 0.5rem;
      justify-content: flex-end;
      flex-wrap: wrap;
    }

    .mobile-card-stat {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 0.5rem 0.75rem;
      background: var(--bg-secondary);
      border-radius: 8px;
      min-width: 60px;
      flex: 1;
      max-width: 80px;
    }

    .mobile-card-stat-value {
      font-size: 1.125rem;
      font-weight: 700;
      color: var(--text-primary);
    }

    .mobile-card-stat-label {
      font-size: 0.6875rem;
      color: var(--text-secondary);
      text-transform: uppercase;
    }

    .mobile-card-actions {
      margin-top: 0.75rem;
      padding-top: 0.75rem;
      border-top: 1px solid var(--border-primary);
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }

    .mobile-card-actions .btn {
      flex: 1;
      justify-content: center;
      min-height: 44px;
      font-size: 0.8125rem;
      padding: 0.625rem 0.75rem;
      min-width: 0;
    }

    .mobile-card-actions .btn i {
      font-size: 0.875rem;
    }

    /* Desktop large screen adjustments */
    @media (min-width: 1400px) {
      .dashboard-main {
        padding: 2.5rem 4rem;
      }

      .table th,
      .table td {
        padding: 1.25rem 2rem;
      }
    }

    @media (min-width: 1600px) {
      .dashboard-main {
        padding: 3rem 5rem;
      }
    }

    /* Tablet landscape: 1024px - 1200px */
    @media (max-width: 1200px) and (min-width: 1024px) {
      .dashboard-main {
        padding: 1.5rem 2rem;
        margin-left: 260px;
        width: calc(100% - 260px);
      }

      .page-header h1 {
        font-size: 1.75rem;
      }

      .filters-grid {
        grid-template-columns: 1fr 1fr;
      }

      .filter-group:last-child {
        grid-column: 1 / -1;
      }
    }

    /* Tablet portrait: 769px - 1023px */
    @media (max-width: 1023px) and (min-width: 769px) {
      .dashboard-main {
        padding: 1.5rem;
        margin-left: 0;
        width: 100%;
      }

      .page-header h1 {
        font-size: 1.75rem;
      }

      .page-header p {
        font-size: 0.875rem;
      }

      .filters-section {
        padding: 1rem;
      }

      .filters-grid {
        grid-template-columns: 1fr;
        gap: 0.75rem;
      }

      .table-wrapper {
        overflow-x: auto;
      }

      .table th,
      .table td {
        padding: 0.75rem 1rem;
        font-size: 0.8125rem;
        white-space: nowrap;
      }

      .table td:first-child {
        white-space: normal;
      }

      .client-avatar {
        width: 32px;
        height: 32px;
      }
    }

    /* Desktop: Show table, hide cards */
    @media (min-width: 769px) {
      .table-container .table {
        display: table;
      }

      .mobile-card-view {
        display: none !important;
      }
    }

    /* Mobile landscape: 481px - 768px */
    @media (max-width: 768px) and (min-width: 481px) {
      .dashboard-main {
        margin-left: 0 !important;
        padding: 1rem 1rem 1rem 1rem !important;
        padding-top: 1rem !important;
        width: 100%;
      }

      .page-header {
        margin-bottom: 1.5rem;
      }

      .page-header h1 {
        font-size: 1.5rem;
        margin-bottom: 0.25rem;
      }

      .page-header p {
        font-size: 0.8125rem;
      }

      .header-actions {
        width: 100%;
      }

      .btn-primary {
        width: 100%;
        justify-content: center;
        padding: 0.75rem 1rem;
      }

      .filters-section {
        padding: 1rem;
        margin-bottom: 1rem;
        border-radius: 8px;
      }

      .filters-grid {
        grid-template-columns: 1fr;
        gap: 0.75rem;
      }

      .filter-input {
        font-size: 16px; /* Prevents zoom on iOS */
      }

      .table-container .table {
        display: none !important;
      }

      .mobile-card-view {
        display: block !important;
        padding: 0.25rem;
      }

      .mobile-card {
        padding: 0.875rem;
      }

      .modal-content-wrapper {
        padding: 1.5rem;
        width: 95%;
        max-height: 85vh;
      }

      .modal-title {
        font-size: 1.5rem;
      }

      .modal-message {
        font-size: 0.9375rem;
      }

      .modal-actions {
        flex-direction: column;
        gap: 0.75rem;
      }

      .modal-btn {
        width: 100%;
        padding: 0.875rem 1.5rem;
      }
    }

    /* Mobile portrait: up to 480px */
    @media (max-width: 480px) {
      .dashboard-main {
        margin-left: 0 !important;
        padding: 1rem 0.75rem 0.75rem 0.75rem !important;
        padding-top: 1rem !important;
        width: 100%;
      }

      .page-header {
        margin-bottom: 1rem;
        flex-direction: column;
        align-items: flex-start;
        gap: 0.75rem;
      }

      .page-header h1 {
        font-size: 1.5rem;
        margin-bottom: 0.25rem;
        line-height: 1.3;
      }

      .page-header p {
        font-size: 0.875rem;
      }

      .header-actions {
        width: 100%;
      }

      .btn-primary {
        width: 100%;
        justify-content: center;
        padding: 0.75rem 1rem;
        font-size: 0.875rem;
      }

      .btn-primary i {
        font-size: 0.875rem;
      }

      .filters-section {
        padding: 0.875rem;
        margin-bottom: 0.875rem;
        border-radius: 8px;
      }

      .filters-grid {
        grid-template-columns: 1fr;
        gap: 0.625rem;
      }

      .filter-group label {
        font-size: 0.8125rem;
      }

      .filter-input {
        padding: 0.75rem;
        font-size: 16px; /* Prevents zoom on iOS */
        border-radius: 8px;
      }

      .btn-filter {
        width: 100%;
        padding: 0.75rem 1rem;
        font-size: 0.875rem;
      }

      .table-container {
        border-radius: 8px;
      }

      .table-container .table {
        display: none !important;
      }

      .mobile-card-view {
        display: block !important;
        padding: 0;
      }

      .mobile-card {
        padding: 0.75rem;
        margin-bottom: 0.625rem;
        border-radius: 8px;
      }

      .mobile-card-header {
        gap: 0.625rem;
        margin-bottom: 0.625rem;
        padding-bottom: 0.625rem;
      }

      .mobile-card-avatar {
        width: 44px;
        height: 44px;
      }

      .mobile-card-title h3 {
        font-size: 0.875rem;
      }

      .mobile-card-title p {
        font-size: 0.75rem;
      }

      .mobile-card-body {
        gap: 0.625rem;
      }

      .mobile-card-row {
        font-size: 0.75rem;
        gap: 0.5rem;
      }

      .mobile-card-label {
        min-width: 55px;
        font-size: 0.75rem;
      }

      .mobile-card-value {
        font-size: 0.75rem;
      }

      .mobile-card-stat {
        padding: 0.5rem 0.625rem;
        min-width: 55px;
        max-width: 70px;
      }

      .mobile-card-stat-value {
        font-size: 1rem;
      }

      .mobile-card-stat-label {
        font-size: 0.625rem;
      }

      .mobile-card-actions {
        margin-top: 0.625rem;
        padding-top: 0.625rem;
        gap: 0.375rem;
        flex-direction: column;
      }

      .mobile-card-actions .btn {
        width: 100%;
        min-height: 42px;
        font-size: 0.8125rem;
        padding: 0.625rem;
      }

      .mobile-card-actions .btn i {
        font-size: 0.8125rem;
      }

      .empty-state {
        padding: 2rem 1rem;
      }

      .empty-state i {
        font-size: 2.5rem;
        margin-bottom: 0.75rem;
      }

      .empty-state p {
        font-size: 0.875rem;
      }

      .modal-content-wrapper {
        padding: 1.25rem;
        width: 95%;
        max-height: 90vh;
        border-radius: 12px;
      }

      .modal-icon {
        width: 64px;
        height: 64px;
        margin-bottom: 1rem;
      }

      .modal-icon i {
        font-size: 2rem;
      }

      .modal-title {
        font-size: 1.25rem;
        margin-bottom: 0.5rem;
      }

      .modal-message {
        font-size: 0.875rem;
        margin-bottom: 1.5rem;
        line-height: 1.5;
      }

      .modal-actions {
        flex-direction: column;
        gap: 0.625rem;
      }

      .modal-btn {
        width: 100%;
        padding: 0.875rem 1.25rem;
        font-size: 0.875rem;
      }
    }

    /* Extra small devices: up to 360px */
    @media (max-width: 360px) {
      .dashboard-main {
        padding: 0.5rem;
      }

      .page-header h1 {
        font-size: 1.125rem;
      }

      .mobile-card {
        padding: 0.625rem;
      }

      .mobile-card-avatar {
        width: 40px;
        height: 40px;
      }

      .mobile-card-title h3 {
        font-size: 0.8125rem;
      }

      .mobile-card-title p {
        font-size: 0.6875rem;
      }

      .mobile-card-row {
        font-size: 0.6875rem;
      }

      .mobile-card-label {
        min-width: 50px;
        font-size: 0.6875rem;
      }

      .mobile-card-value {
        font-size: 0.6875rem;
      }

      .mobile-card-stat {
        padding: 0.375rem 0.5rem;
        min-width: 50px;
        max-width: 65px;
      }

      .mobile-card-stat-value {
        font-size: 0.9375rem;
      }

      .mobile-card-stat-label {
        font-size: 0.5625rem;
      }
    }

    /* Success/Error/Confirmation Modal Styles */
    .success-modal,
    .confirmation-modal {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 10000;
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s ease;
    }

    .success-modal.show,
    .confirmation-modal.show {
      opacity: 1;
      visibility: visible;
    }

    .modal-content-wrapper {
      background: white;
      border-radius: 16px;
      padding: 2.5rem;
      max-width: 480px;
      width: 90%;
      max-height: 90vh;
      overflow-y: auto;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      transform: scale(0.8);
      transition: transform 0.3s ease;
      text-align: center;
    }

    .success-modal.show .modal-content-wrapper,
    .confirmation-modal.show .modal-content-wrapper {
      transform: scale(1);
    }

    .modal-icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.5rem;
      animation: successPulse 0.6s ease-out;
    }

    .modal-icon.error {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
    }

    .modal-icon.warning {
      background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    }

    .modal-icon i {
      color: white;
      font-size: 2.5rem;
    }

    @keyframes successPulse {
      0% {
        transform: scale(0);
        opacity: 0;
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    .modal-title {
      font-size: 1.75rem;
      font-weight: 700;
      color: #111827;
      margin-bottom: 0.75rem;
    }

    .modal-message {
      color: #6b7280;
      font-size: 1rem;
      margin-bottom: 2rem;
      line-height: 1.6;
    }

    .modal-actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
    }

    .modal-btn {
      padding: 0.75rem 2rem;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      border: none;
      font-size: 0.9375rem;
      transition: all 0.2s;
      text-decoration: none;
      display: inline-block;
    }

    .modal-btn-primary {
      background: linear-gradient(135deg, #c51d34 0%, #a01628 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(197, 29, 52, 0.3);
    }

    .modal-btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(197, 29, 52, 0.4);
    }

    .modal-btn-secondary {
      background: #f3f4f6;
      color: #374151;
    }

    .modal-btn-secondary:hover {
      background: #e5e7eb;
    }

  </style>
</head>
<body>
  <div class="dashboard-layout">
    <%- include('../partials/admin-sidebar') %>
    
    <div class="dashboard-main">
      <div class="page-header">
        <div>
          <h1>Manage Clients</h1>
          <p>View and manage all clients in the system</p>
        </div>
        <div class="header-actions">
          <a href="/admin/clients/add" class="btn-primary">
            <i class="fas fa-plus"></i> Add Client
          </a>
        </div>
      </div>

      <div class="filters-section">
        <form method="GET" action="/admin/clients" class="filters-grid">
          <div class="filter-group">
            <label for="search">Search by Name or Email</label>
            <input 
              type="text" 
              id="search" 
              name="search" 
              class="filter-input" 
              placeholder="Search clients..."
              value="<%= filters.search %>"
            >
          </div>
          <div class="filter-group">
            <label for="brand_id">Filter by Brand</label>
            <select id="brand_id" name="brand_id" class="filter-input">
              <option value="">All Brands</option>
              <% if (brands && brands.length > 0) { %>
                <% brands.forEach(brand => { %>
                  <option value="<%= brand.id %>" <%= filters.brand_id == brand.id ? 'selected' : '' %>>
                    <%= brand.name %>
                  </option>
                <% }) %>
              <% } %>
            </select>
          </div>
          <div class="filter-group">
            <button type="submit" class="btn btn-filter">
              <i class="fas fa-search"></i> Search
            </button>
          </div>
        </form>
      </div>

      <div class="table-container">
        <% if (clients && clients.length > 0) { %>
          <!-- Desktop Table View -->
          <div class="table-wrapper">
          <table class="table">
            <thead>
              <tr>
                <th>Client</th>
                <th>Email</th>
                <th>Brand</th>
                <th>Projects</th>
                <th>Inquiries</th>
                <th>Last Active</th>
                <th>Joined</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% clients.forEach(client => { %>
                <tr>
                  <td>
                    <div style="display: flex; align-items: center; gap: 0.75rem; min-width: 0;">
                      <img src="<%= client.avatar || '/images/default-avatar.webp' %>" alt="<%= client.name %>" class="client-avatar" onerror="this.onerror=null; this.src='/images/default-avatar.webp';" referrerpolicy="no-referrer">
                      <span style="overflow: hidden; text-overflow: ellipsis; white-space: nowrap;"><%= client.name %></span>
                    </div>
                  </td>
                  <td style="overflow: hidden; text-overflow: ellipsis; white-space: nowrap; max-width: 200px;"><%= client.email %></td>
                  <td>
                    <% if (client.brand_name) { %>
                      <div style="display: flex; align-items: center; gap: 0.5rem; min-width: 0;">
                        <% if (client.brand_logo) { %>
                          <img src="<%= client.brand_logo %>" alt="<%= client.brand_name %>" style="width: 24px; height: 24px; object-fit: contain; flex-shrink: 0;" onerror="this.style.display='none';">
                        <% } %>
                        <span style="overflow: hidden; text-overflow: ellipsis; white-space: nowrap;"><%= client.brand_name %></span>
                      </div>
                    <% } else { %>
                      <span style="color: var(--text-secondary);">—</span>
                    <% } %>
                  </td>
                  <td><%= client.project_count || 0 %></td>
                  <td><%= client.inquiry_count || 0 %></td>
                  <td>
                    <% if (client.last_active) { %>
                      <span title="<%= new Date(client.last_active).toLocaleString() %>">
                        <%= new Date(client.last_active).toLocaleDateString() %><br>
                        <small style="color: var(--text-secondary); font-size: 0.75rem;" data-time="<%= client.last_active %>"><%= new Date(client.last_active).toLocaleTimeString() %></small>
                      </span>
                    <% } else { %>
                      <span style="color: var(--text-secondary);">Never</span>
                    <% } %>
                  </td>
                  <td><%= new Date(client.created_at).toLocaleDateString() %></td>
                  <td>
                    <div class="action-buttons">
                      <a href="/admin/clients/view/<%= client.id %>" class="btn btn-info btn-sm btn-icon-only" title="View Client">
                        <i class="fas fa-eye"></i>
                      </a>
                      <a href="/admin/clients/edit/<%= client.id %>" class="btn btn-secondary btn-sm btn-icon-only" title="Edit Client">
                        <i class="fas fa-edit"></i>
                      </a>
                      <button class="btn btn-danger btn-sm btn-icon-only delete-client" data-client-id="<%= client.id %>" data-client-name="<%= client.name %>" title="Delete Client">
                        <i class="fas fa-trash"></i>
                      </button>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
          </div>

          <!-- Mobile Card View -->
          <div class="mobile-card-view">
            <% clients.forEach(client => { %>
              <div class="mobile-card">
                <div class="mobile-card-header">
                  <img src="<%= client.avatar || '/images/default-avatar.webp' %>" alt="<%= client.name %>" class="mobile-card-avatar" onerror="this.onerror=null; this.src='/images/default-avatar.webp';" referrerpolicy="no-referrer">
                  <div class="mobile-card-title">
                    <h3><%= client.name %></h3>
                    <p><%= client.email %></p>
                  </div>
                </div>
                <div class="mobile-card-body">
                  <div class="mobile-card-row">
                    <span class="mobile-card-label">Brand:</span>
                    <span class="mobile-card-value">
                      <% if (client.brand_name) { %>
                        <div class="mobile-card-brand">
                          <% if (client.brand_logo) { %>
                            <img src="<%= client.brand_logo %>" alt="<%= client.brand_name %>" class="mobile-card-brand-logo" onerror="this.style.display='none';">
                          <% } %>
                          <span style="overflow: hidden; text-overflow: ellipsis; white-space: nowrap; max-width: 150px;"><%= client.brand_name %></span>
                        </div>
                      <% } else { %>
                        <span style="color: var(--text-secondary);">—</span>
                      <% } %>
                    </span>
                  </div>
                  <div class="mobile-card-row">
                    <span class="mobile-card-label">Last Active:</span>
                    <span class="mobile-card-value">
                      <% if (client.last_active) { %>
                        <%= new Date(client.last_active).toLocaleDateString() %> <%= new Date(client.last_active).toLocaleTimeString() %>
                      <% } else { %>
                        <span style="color: var(--text-secondary);">Never</span>
                      <% } %>
                    </span>
                  </div>
                  <div class="mobile-card-row">
                    <span class="mobile-card-label">Joined:</span>
                    <span class="mobile-card-value"><%= new Date(client.created_at).toLocaleDateString() %></span>
                  </div>
                  <div class="mobile-card-row">
                    <span class="mobile-card-label">Stats:</span>
                    <span class="mobile-card-value">
                      <div class="mobile-card-stats">
                        <div class="mobile-card-stat">
                          <span class="mobile-card-stat-value"><%= client.project_count || 0 %></span>
                          <span class="mobile-card-stat-label">Projects</span>
                        </div>
                        <div class="mobile-card-stat">
                          <span class="mobile-card-stat-value"><%= client.inquiry_count || 0 %></span>
                          <span class="mobile-card-stat-label">Inquiries</span>
                        </div>
                      </div>
                    </span>
                  </div>
                </div>
                <div class="mobile-card-actions">
                  <a href="/admin/clients/view/<%= client.id %>" class="btn btn-info" style="text-decoration: none;">
                    <i class="fas fa-eye"></i> View
                  </a>
                  <a href="/admin/clients/edit/<%= client.id %>" class="btn btn-secondary" style="text-decoration: none;">
                    <i class="fas fa-edit"></i> Edit
                  </a>
                  <button class="btn btn-danger delete-client" data-client-id="<%= client.id %>" data-client-name="<%= client.name %>">
                    <i class="fas fa-trash"></i>
                  </button>
                </div>
              </div>
            <% }) %>
          </div>
        <% } else { %>
          <div class="empty-state">
            <i class="fas fa-users"></i>
            <p>No clients found</p>
          </div>
        <% } %>
      </div>

      <!-- Pagination -->
      <% if (pagination && pagination.totalPages > 1) { %>
        <div class="pagination-container">
          <div class="pagination-info">
            Showing <%= ((pagination.currentPage - 1) * pagination.limit) + 1 %> to <%= Math.min(pagination.currentPage * pagination.limit, pagination.totalCount) %> of <%= pagination.totalCount %> results
          </div>
          <div class="pagination">
            <% 
              const buildQuery = (page) => {
                const params = [];
                if (filters.search) params.push(`search=${encodeURIComponent(filters.search)}`);
                if (filters.brand_id) params.push(`brand_id=${encodeURIComponent(filters.brand_id)}`);
                params.push(`page=${page}`);
                return params.length > 0 ? '?' + params.join('&') : '';
              };
            %>
            <% if (pagination.hasPrev) { %>
              <a href="/admin/clients<%= buildQuery(pagination.currentPage - 1) %>" class="pagination-btn">
                <i class="fas fa-chevron-left"></i>
              </a>
            <% } else { %>
              <span class="pagination-btn disabled">
                <i class="fas fa-chevron-left"></i>
              </span>
            <% } %>

            <% 
              let startPage = Math.max(1, pagination.currentPage - 2);
              let endPage = Math.min(pagination.totalPages, pagination.currentPage + 2);
              if (endPage - startPage < 4) {
                if (startPage === 1) {
                  endPage = Math.min(5, pagination.totalPages);
                } else {
                  startPage = Math.max(1, pagination.totalPages - 4);
                }
              }
            %>

            <% if (startPage > 1) { %>
              <a href="/admin/clients<%= buildQuery(1) %>" class="pagination-btn">1</a>
              <% if (startPage > 2) { %>
                <span class="pagination-ellipsis">...</span>
              <% } %>
            <% } %>

            <% for (let i = startPage; i <= endPage; i++) { %>
              <% if (i === pagination.currentPage) { %>
                <span class="pagination-btn active"><%= i %></span>
              <% } else { %>
                <a href="/admin/clients<%= buildQuery(i) %>" class="pagination-btn"><%= i %></a>
              <% } %>
            <% } %>

            <% if (endPage < pagination.totalPages) { %>
              <% if (endPage < pagination.totalPages - 1) { %>
                <span class="pagination-ellipsis">...</span>
              <% } %>
              <a href="/admin/clients<%= buildQuery(pagination.totalPages) %>" class="pagination-btn"><%= pagination.totalPages %></a>
            <% } %>

            <% if (pagination.hasNext) { %>
              <a href="/admin/clients<%= buildQuery(pagination.currentPage + 1) %>" class="pagination-btn">
                <i class="fas fa-chevron-right"></i>
              </a>
            <% } else { %>
              <span class="pagination-btn disabled">
                <i class="fas fa-chevron-right"></i>
              </span>
            <% } %>
          </div>
        </div>
      <% } %>
    </div>
  </div>

  <script>

    function formatRelativeTime(dateString) {
      if (!dateString) return 'Never';
      const date = new Date(dateString);
      const now = new Date();
      const diffMs = now - date;
      const diffMins = Math.floor(diffMs / 60000);
      const diffHours = Math.floor(diffMs / 3600000);
      const diffDays = Math.floor(diffMs / 86400000);
      
      if (diffMins < 1) return 'Just now';
      if (diffMins < 60) return `${diffMins} minute${diffMins !== 1 ? 's' : ''} ago`;
      if (diffHours < 24) return `${diffHours} hour${diffHours !== 1 ? 's' : ''} ago`;
      if (diffDays < 7) return `${diffDays} day${diffDays !== 1 ? 's' : ''} ago`;
      return date.toLocaleDateString();
    }

    function showModal(modalId) {
      const modal = document.getElementById(modalId);
      if (modal) {
        modal.classList.add('show');
      }
    }

    function closeModal(modalId) {
      const modal = document.getElementById(modalId);
      if (modal) {
        modal.classList.remove('show');
      }
    }


    function showConfirmation(message, onConfirm) {
      const modal = document.getElementById('confirmationModal');
      const messageEl = document.getElementById('confirmationMessage');
      const confirmBtn = document.getElementById('confirmBtn');
      
      if (modal && messageEl) {
        messageEl.textContent = message;
        modal.classList.add('show');
        

        const newConfirmBtn = confirmBtn.cloneNode(true);
        confirmBtn.parentNode.replaceChild(newConfirmBtn, confirmBtn);
        
        newConfirmBtn.onclick = function() {
          closeModal('confirmationModal');
          if (onConfirm) onConfirm();
        };
      }
    }


    document.querySelectorAll('.delete-client').forEach(btn => {
      btn.addEventListener('click', async function() {
        const clientId = this.dataset.clientId;
        const clientName = this.dataset.clientName;

        showConfirmation(`Are you sure you want to delete client "${clientName}"? This action cannot be undone.`, async function() {
          try {
            const response = await fetch(`/admin/clients/${clientId}`, {
              method: 'DELETE'
            });

            const data = await response.json();

            if (data.success) {
              document.getElementById('successMessage').textContent = 'Client deleted successfully';
              showModal('successModal');
              setTimeout(() => {
                location.reload();
              }, 1500);
            } else {
              document.getElementById('errorMessage').textContent = data.message || 'Error deleting client';
              showModal('errorModal');
            }
          } catch (error) {
            console.error('Error:', error);
            document.getElementById('errorMessage').textContent = 'An error occurred while deleting the client';
            showModal('errorModal');
          }
        });
      });
    });


    document.addEventListener('click', function(e) {
      if (e.target.classList.contains('success-modal') || e.target.classList.contains('confirmation-modal')) {
        closeModal(e.target.id);
      }
    });


    document.addEventListener('DOMContentLoaded', function() {
      document.querySelectorAll('[data-time]').forEach(el => {
        const timeStr = el.getAttribute('data-time');
        if (timeStr) {
          const relativeTime = formatRelativeTime(timeStr);
          if (relativeTime !== 'Never' && !relativeTime.includes('/')) {
            el.textContent = relativeTime;
          }
        }
      });
    });


    document.addEventListener('DOMContentLoaded', function() {
      document.querySelectorAll('[data-time]').forEach(el => {
        const timeStr = el.getAttribute('data-time');
        if (timeStr) {
          const relativeTime = formatRelativeTime(timeStr);
          if (relativeTime !== 'Never' && !relativeTime.includes('/')) {
            el.textContent = relativeTime;
          }
        }
      });
    });
  </script>

  <!-- Success Modal -->
  <div class="success-modal" id="successModal">
    <div class="modal-content-wrapper">
      <div class="modal-icon">
        <i class="fas fa-check"></i>
      </div>
      <h3 class="modal-title">Success!</h3>
      <p class="modal-message" id="successMessage">Operation completed successfully.</p>
      <div class="modal-actions">
        <button onclick="closeModal('successModal')" class="modal-btn modal-btn-primary">OK</button>
      </div>
    </div>
  </div>

  <!-- Error Modal -->
  <div class="success-modal" id="errorModal">
    <div class="modal-content-wrapper">
      <div class="modal-icon error">
        <i class="fas fa-exclamation-triangle"></i>
      </div>
      <h3 class="modal-title">Error Occurred</h3>
      <p class="modal-message" id="errorMessage">Something went wrong. Please try again.</p>
      <div class="modal-actions">
        <button onclick="closeModal('errorModal')" class="modal-btn modal-btn-primary">Close</button>
      </div>
    </div>
  </div>

  <!-- Confirmation Modal -->
  <div class="confirmation-modal" id="confirmationModal">
    <div class="modal-content-wrapper">
      <div class="modal-icon warning">
        <i class="fas fa-question-circle"></i>
      </div>
      <h3 class="modal-title">Confirm Action</h3>
      <p class="modal-message" id="confirmationMessage">Are you sure you want to proceed?</p>
      <div class="modal-actions">
        <button onclick="closeModal('confirmationModal')" class="modal-btn modal-btn-secondary">Cancel</button>
        <button id="confirmBtn" class="modal-btn modal-btn-primary">Confirm</button>
      </div>
    </div>
  </div>
</body>
</html>

