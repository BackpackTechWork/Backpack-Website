<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      margin: 0;
      padding: 0;
      margin-top: 0;
      padding-top: 0;
    }

    :root {
      --bg-primary: #ffffff;
      --bg-secondary: #f9fafb;
      --border-primary: #e5e7eb;
      --text-primary: #111827;
      --text-secondary: #6b7280;
      --accent-red: #c51d34;
    }

    .dashboard-layout {
      display: flex;
      min-height: 100vh;
      margin: 0;
      padding: 0;
      margin-top: 0;
      padding-top: 0;
    }

    .dashboard-main {
      flex: 1;
      padding: 0.25rem 2.5rem 2rem 2.5rem;
      margin-left: 280px;
      background: var(--bg-secondary);
      width: calc(100% - 280px);
      min-width: 0;
      margin-top: 0;
      padding-top: 0.25rem;
    }

    .page-header {
      margin-bottom: 2rem;
      margin-top: 0;
      padding-top: 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .page-header h1 {
      font-size: 2rem;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 0.5rem;
      line-height: 1.2;
    }

    .page-header p {
      color: var(--text-secondary);
      font-size: 0.9375rem;
      margin: 0;
    }

    .btn-primary {
      background: var(--accent-red);
      color: white;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      border: none;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      white-space: nowrap;
      touch-action: manipulation;
      text-decoration: none;
    }

    .btn-primary:hover {
      background: #a01628;
    }

    .table-container {
      background: var(--bg-primary);
      border-radius: 12px;
      box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.05);
      overflow: hidden;
      margin-bottom: 2rem;
      width: 100%;
      max-width: 100%;
    }

    .table {
      width: 100%;
      border-collapse: collapse;
      table-layout: auto;
    }

    .table-wrapper {
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
    }

    .table thead {
      background: var(--bg-secondary);
    }

    .table th {
      padding: 1rem 1.5rem;
      text-align: left;
      font-weight: 600;
      font-size: 0.875rem;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .table td {
      padding: 1rem 1.5rem;
      border-top: 1px solid var(--border-primary);
      color: var(--text-primary);
    }

    .table tbody tr:hover {
      background: var(--bg-secondary);
    }

    .status-badge {
      display: inline-block;
      padding: 0.25rem 0.75rem;
      border-radius: 6px;
      font-size: 0.8125rem;
      font-weight: 600;
    }

    .status-badge.active {
      background: rgba(34, 197, 94, 0.1);
      color: #22c55e;
    }

    .status-badge.inactive {
      background: rgba(239, 68, 68, 0.1);
      color: #ef4444;
    }

    .btn {
      padding: 0.5rem 1rem;
      border-radius: 6px;
      font-size: 0.875rem;
      font-weight: 500;
      cursor: pointer;
      border: none;
      transition: all 0.2s;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      touch-action: manipulation;
      -webkit-tap-highlight-color: transparent;
    }

    .btn-sm {
      padding: 0.5rem;
      font-size: 0.875rem;
      width: 36px;
      height: 36px;
      justify-content: center;
    }

    .btn-icon-only {
      padding: 0.5rem;
      width: 36px;
      height: 36px;
      gap: 0;
    }

    .btn-danger {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
    }

    .btn-danger:hover {
      background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(239, 68, 68, 0.4);
    }

    .btn-secondary {
      background: linear-gradient(135deg, #6b7280 0%, #4b5563 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(107, 114, 128, 0.3);
    }

    .btn-secondary:hover {
      background: linear-gradient(135deg, #4b5563 0%, #374151 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(107, 114, 128, 0.4);
    }

    .btn-success {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
    }

    .btn-success:hover {
      background: linear-gradient(135deg, #059669 0%, #047857 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(16, 185, 129, 0.4);
    }

    .action-buttons {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }

    .action-buttons .btn {
      min-width: 36px;
      min-height: 36px;
      touch-action: manipulation;
    }

    .empty-state {
      padding: 4rem 2rem;
      text-align: center;
      color: var(--text-secondary);
    }

    .empty-state i {
      font-size: 4rem;
      margin-bottom: 1.5rem;
      opacity: 0.3;
      color: var(--text-secondary);
    }

    .empty-state h3 {
      font-size: 1.25rem;
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 0.5rem;
    }

    .empty-state p {
      font-size: 0.9375rem;
      margin: 0;
      color: var(--text-secondary);
    }

    .pagination-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 1.5rem;
      padding: 1rem 0;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .pagination-info {
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    .pagination {
      display: flex;
      gap: 0.5rem;
      align-items: center;
      flex-wrap: wrap;
    }

    .pagination-btn {
      padding: 0.5rem 0.75rem;
      border-radius: 6px;
      background: var(--bg-primary);
      border: 1px solid var(--border-primary);
      color: var(--text-primary);
      text-decoration: none;
      font-size: 0.875rem;
      font-weight: 500;
      transition: all 0.2s;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      cursor: pointer;
    }

    .pagination-btn:hover:not(.disabled):not(.active) {
      background: var(--bg-secondary);
      border-color: var(--accent-red);
      color: var(--accent-red);
    }

    .pagination-btn.active {
      background: var(--accent-red);
      color: white;
      border-color: var(--accent-red);
    }

    .pagination-btn.disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

    .pagination-ellipsis {
      padding: 0.5rem 0.25rem;
      color: var(--text-secondary);
    }

    @media (max-width: 768px) {
      .pagination-container {
        flex-direction: column;
        align-items: stretch;
      }

      .pagination-info {
        text-align: center;
      }

      .pagination {
        justify-content: center;
      }
    }

    /* Mobile Card Layout */
    .mobile-card-view {
      display: none;
      padding: 0.5rem;
    }

    .mobile-card {
      background: var(--bg-primary);
      border: 1px solid var(--border-primary);
      border-radius: 12px;
      padding: 1rem;
      margin-bottom: 0.75rem;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
      transition: all 0.2s ease;
    }

    .mobile-card:hover {
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
    }

    .mobile-card-header {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      margin-bottom: 0.75rem;
      padding-bottom: 0.75rem;
      border-bottom: 1px solid var(--border-primary);
      flex-wrap: wrap;
    }

    .mobile-card-icon {
      width: 44px;
      height: 44px;
      border-radius: 10px;
      background: linear-gradient(135deg, #c51d34 0%, #a01628 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 1.125rem;
      flex-shrink: 0;
    }

    .mobile-card-icon img {
      width: 24px;
      height: 24px;
      object-fit: contain;
    }

    .mobile-card-title {
      flex: 1;
      min-width: 0;
    }

    .mobile-card-title h3 {
      margin: 0 0 0.25rem 0;
      font-size: 0.9375rem;
      font-weight: 700;
      color: var(--text-primary);
      overflow: hidden;
      text-overflow: ellipsis;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      word-break: break-word;
      line-height: 1.3;
    }

    .mobile-card-title p {
      margin: 0;
      font-size: 0.8125rem;
      color: var(--text-secondary);
    }

    .mobile-card-body {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .mobile-card-row {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 0.75rem;
      font-size: 0.8125rem;
    }

    .mobile-card-label {
      color: var(--text-secondary);
      font-weight: 600;
      min-width: 60px;
      flex-shrink: 0;
      font-size: 0.8125rem;
    }

    .mobile-card-value {
      color: var(--text-primary);
      text-align: right;
      flex: 1;
      word-break: break-word;
      font-size: 0.8125rem;
    }

    .mobile-card-actions {
      margin-top: 0.75rem;
      padding-top: 0.75rem;
      border-top: 1px solid var(--border-primary);
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }

    .mobile-card-actions .btn {
      flex: 1;
      justify-content: center;
      min-height: 44px;
      font-size: 0.8125rem;
      padding: 0.625rem 0.75rem;
      min-width: 0;
    }

    .mobile-card-actions .btn i {
      font-size: 0.875rem;
    }

    /* Desktop large screen adjustments */
    @media (min-width: 1400px) {
      .dashboard-main {
        padding: 2.5rem 4rem;
      }

      .table th,
      .table td {
        padding: 1.25rem 2rem;
      }
    }

    @media (min-width: 1600px) {
      .dashboard-main {
        padding: 3rem 5rem;
      }
    }

    /* Tablet landscape: 1024px - 1200px */
    @media (max-width: 1200px) and (min-width: 1024px) {
      .dashboard-main {
        padding: 1.5rem 2rem;
        margin-left: 260px;
        width: calc(100% - 260px);
      }

      .page-header h1 {
        font-size: 1.75rem;
      }

      .search-form {
        flex-wrap: wrap;
      }
    }

    /* Tablet portrait: 769px - 1023px */
    @media (max-width: 1023px) and (min-width: 769px) {
      .dashboard-main {
        padding: 1.5rem;
        margin-left: 0;
        width: 100%;
      }

      .page-header h1 {
        font-size: 1.75rem;
      }

      .page-header p {
        font-size: 0.875rem;
      }

      .search-container {
        padding: 1rem;
      }

      .search-form {
        flex-direction: column;
        gap: 0.75rem;
      }

      .search-input-wrapper {
        width: 100%;
      }

      .table-wrapper {
        overflow-x: auto;
      }

      .table th,
      .table td {
        padding: 0.75rem 1rem;
        font-size: 0.8125rem;
        white-space: nowrap;
      }

      .table td:first-child {
        white-space: normal;
      }
    }

    /* Desktop: Show table, hide cards */
    @media (min-width: 769px) {
      .table-container .table {
        display: table;
      }

      .mobile-card-view {
        display: none !important;
      }
    }

    /* Mobile landscape: 481px - 768px */
    @media (max-width: 768px) and (min-width: 481px) {
      .dashboard-main {
        margin-left: 0 !important;
        padding: 1rem 1rem 1rem 1rem !important;
        padding-top: 1rem !important;
        width: 100%;
      }

      .page-header {
        margin-bottom: 1.5rem;
      }

      .page-header h1 {
        font-size: 1.5rem;
        margin-bottom: 0.25rem;
      }

      .page-header p {
        font-size: 0.8125rem;
      }

      .page-header > div:last-child {
        width: 100%;
        flex-direction: column;
        gap: 0.75rem;
      }

      .page-header > div:last-child > * {
        width: 100%;
      }

      .search-container {
        padding: 1rem;
        margin-bottom: 1rem;
        border-radius: 8px;
      }

      .search-form {
        flex-direction: column;
        gap: 0.75rem;
      }

      .search-input-wrapper {
        width: 100%;
      }

      .search-input {
        font-size: 16px; /* Prevents zoom on iOS */
      }

      .search-form .btn-primary,
      .search-form .btn-secondary {
        width: 100%;
        justify-content: center;
      }

      .table-container .table {
        display: none !important;
      }

      .mobile-card-view {
        display: block !important;
        padding: 0.25rem;
      }

      .mobile-card {
        padding: 0.875rem;
      }

      .modal-content-wrapper {
        padding: 1.5rem;
        width: 95%;
        max-height: 85vh;
      }

      .modal-title {
        font-size: 1.5rem;
      }

      .modal-message {
        font-size: 0.9375rem;
      }

      .modal-actions {
        flex-direction: column;
        gap: 0.75rem;
      }

      .modal-btn {
        width: 100%;
        padding: 0.875rem 1.5rem;
      }

      .reorder-modal-content {
        padding: 1.5rem;
        width: 95%;
        max-height: 85vh;
      }

      .reorder-list {
        min-height: 250px;
        max-height: 350px;
      }

      .reorder-item {
        padding: 0.875rem;
        gap: 0.75rem;
      }

      .reorder-item-icon {
        width: 40px;
        height: 40px;
        font-size: 1.25rem;
      }

      .reorder-modal-actions {
        flex-direction: column;
        gap: 0.75rem;
      }

      .reorder-modal-actions .btn {
        width: 100%;
      }
    }

    /* Mobile portrait: up to 480px */
    @media (max-width: 480px) {
      .dashboard-main {
        margin-left: 0 !important;
        padding: 1rem 0.75rem 0.75rem 0.75rem !important;
        padding-top: 1rem !important;
        width: 100%;
      }

      .page-header {
        margin-bottom: 1rem;
        flex-direction: column;
        align-items: flex-start;
        gap: 0.75rem;
      }

      .page-header h1 {
        font-size: 1.5rem;
      }

      .page-header p {
        font-size: 0.875rem;
      }

      .page-header h1 {
        font-size: 1.25rem;
        margin-bottom: 0.25rem;
        line-height: 1.3;
      }

      .page-header p {
        font-size: 0.75rem;
      }

      .page-header > div:last-child {
        width: 100%;
        flex-direction: column;
        gap: 0.625rem;
      }

      .page-header > div:last-child > * {
        width: 100%;
      }

      .btn-primary {
        width: 100%;
        justify-content: center;
        padding: 0.75rem 1rem;
        font-size: 0.875rem;
      }

      .btn-primary i {
        font-size: 0.875rem;
      }

      .search-container {
        padding: 0.875rem;
        margin-bottom: 0.875rem;
        border-radius: 8px;
      }

      .search-form {
        flex-direction: column;
        gap: 0.625rem;
      }

      .search-input-wrapper {
        width: 100%;
      }

      .search-input {
        padding: 0.75rem 0.875rem 0.75rem 2.25rem;
        font-size: 16px; /* Prevents zoom on iOS */
        border-radius: 8px;
      }

      .search-input-wrapper i {
        left: 0.875rem;
        font-size: 0.875rem;
      }

      .search-form .btn-primary,
      .search-form .btn-secondary {
        width: 100%;
        justify-content: center;
        padding: 0.75rem 1rem;
      }

      .table-container {
        border-radius: 8px;
      }

      .table-container .table {
        display: none !important;
      }

      .mobile-card-view {
        display: block !important;
        padding: 0;
      }

      .mobile-card {
        padding: 0.75rem;
        margin-bottom: 0.625rem;
        border-radius: 8px;
      }

      .mobile-card-header {
        gap: 0.625rem;
        margin-bottom: 0.625rem;
        padding-bottom: 0.625rem;
      }

      .mobile-card-icon {
        width: 40px;
        height: 40px;
        font-size: 1rem;
      }

      .mobile-card-title h3 {
        font-size: 0.875rem;
      }

      .mobile-card-title p {
        font-size: 0.75rem;
      }

      .mobile-card-body {
        gap: 0.625rem;
      }

      .mobile-card-row {
        font-size: 0.75rem;
        gap: 0.5rem;
      }

      .mobile-card-label {
        min-width: 55px;
        font-size: 0.75rem;
      }

      .mobile-card-value {
        font-size: 0.75rem;
      }

      .status-badge {
        font-size: 0.75rem;
        padding: 0.25rem 0.625rem;
      }

      .mobile-card-actions {
        margin-top: 0.625rem;
        padding-top: 0.625rem;
        gap: 0.375rem;
        flex-direction: column;
      }

      .mobile-card-actions .btn {
        width: 100%;
        min-height: 42px;
        font-size: 0.8125rem;
        padding: 0.625rem;
      }

      .mobile-card-actions .btn i {
        font-size: 0.8125rem;
      }

      .empty-state {
        padding: 2rem 1rem;
      }

      .empty-state i {
        font-size: 2.5rem;
        margin-bottom: 0.75rem;
      }

      .empty-state h3 {
        font-size: 1.125rem;
      }

      .empty-state p {
        font-size: 0.875rem;
      }

      .modal-content-wrapper {
        padding: 1.25rem;
        width: 95%;
        max-height: 90vh;
        border-radius: 12px;
      }

      .modal-icon {
        width: 64px;
        height: 64px;
        margin-bottom: 1rem;
      }

      .modal-icon i {
        font-size: 2rem;
      }

      .modal-title {
        font-size: 1.25rem;
        margin-bottom: 0.5rem;
      }

      .modal-message {
        font-size: 0.875rem;
        margin-bottom: 1.5rem;
        line-height: 1.5;
      }

      .modal-actions {
        flex-direction: column;
        gap: 0.625rem;
      }

      .modal-btn {
        width: 100%;
        padding: 0.875rem 1.25rem;
        font-size: 0.875rem;
      }

      .reorder-modal-content {
        padding: 1.25rem;
        width: 95%;
        max-height: 90vh;
        border-radius: 12px;
      }

      .reorder-modal-header h2 {
        font-size: 1.25rem;
      }

      .reorder-modal-header p {
        font-size: 0.8125rem;
      }

      .reorder-list {
        min-height: 200px;
        max-height: 300px;
        margin-bottom: 1rem;
      }

      .reorder-item {
        padding: 0.75rem;
        gap: 0.625rem;
        margin-bottom: 0.5rem;
      }

      .reorder-handle {
        font-size: 1rem;
      }

      .reorder-item-icon {
        width: 36px;
        height: 36px;
        font-size: 1rem;
      }

      .reorder-item-name {
        font-size: 0.875rem;
      }

      .reorder-item-description {
        font-size: 0.75rem;
      }

      .reorder-item-order {
        font-size: 0.75rem;
        min-width: 30px;
      }

      .reorder-modal-actions {
        flex-direction: column;
        gap: 0.625rem;
        padding-top: 1rem;
      }

      .reorder-modal-actions .btn {
        width: 100%;
        padding: 0.875rem 1.25rem;
        font-size: 0.875rem;
      }
    }

    /* Extra small devices: up to 360px */
    @media (max-width: 360px) {
      .dashboard-main {
        padding: 0.5rem;
      }

      .page-header h1 {
        font-size: 1.125rem;
      }

      .mobile-card {
        padding: 0.625rem;
      }

      .mobile-card-icon {
        width: 36px;
        height: 36px;
        font-size: 0.9375rem;
      }

      .mobile-card-title h3 {
        font-size: 0.8125rem;
      }

      .mobile-card-title p {
        font-size: 0.6875rem;
      }

      .mobile-card-row {
        font-size: 0.6875rem;
      }

      .mobile-card-label {
        min-width: 50px;
        font-size: 0.6875rem;
      }

      .mobile-card-value {
        font-size: 0.6875rem;
      }

      .reorder-item {
        padding: 0.625rem;
        gap: 0.5rem;
      }

      .reorder-item-icon {
        width: 32px;
        height: 32px;
        font-size: 0.875rem;
      }
    }

    .search-container {
      background: var(--bg-primary);
      border-radius: 12px;
      box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.05);
      padding: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .search-form {
      display: flex;
      gap: 1rem;
      align-items: center;
    }

    .search-input-wrapper {
      flex: 1;
      position: relative;
    }

    .search-input-wrapper i {
      position: absolute;
      left: 1rem;
      top: 50%;
      transform: translateY(-50%);
      color: var(--text-secondary);
      pointer-events: none;
    }

    .search-input {
      width: 100%;
      padding: 0.75rem 1rem 0.75rem 2.5rem;
      border: 2px solid var(--border-primary);
      border-radius: 8px;
      font-size: 0.875rem;
      transition: all 0.2s;
      font-family: inherit;
      box-sizing: border-box;
    }

    .search-input:focus {
      outline: none;
      border-color: var(--accent-red);
      box-shadow: 0 0 0 3px rgba(197, 29, 52, 0.1);
    }

    /* Success/Error/Confirmation Modal Styles */
    .success-modal,
    .confirmation-modal {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 10000;
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s ease;
    }

    .success-modal.show,
    .confirmation-modal.show {
      opacity: 1;
      visibility: visible;
    }

    .modal-content-wrapper {
      background: white;
      border-radius: 16px;
      padding: 2.5rem;
      max-width: 480px;
      width: 90%;
      max-height: 90vh;
      overflow-y: auto;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      transform: scale(0.8);
      transition: transform 0.3s ease;
      text-align: center;
    }

    .success-modal.show .modal-content-wrapper,
    .confirmation-modal.show .modal-content-wrapper {
      transform: scale(1);
    }

    .modal-icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.5rem;
      animation: successPulse 0.6s ease-out;
    }

    .modal-icon.error {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
    }

    .modal-icon.warning {
      background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    }

    .modal-icon i {
      color: white;
      font-size: 2.5rem;
    }

    @keyframes successPulse {
      0% {
        transform: scale(0);
        opacity: 0;
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    .modal-title {
      font-size: 1.75rem;
      font-weight: 700;
      color: #111827;
      margin-bottom: 0.75rem;
    }

    .modal-message {
      color: #6b7280;
      font-size: 1rem;
      margin-bottom: 2rem;
      line-height: 1.6;
    }

    .modal-actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
    }

    .modal-btn {
      padding: 0.75rem 2rem;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      border: none;
      font-size: 0.9375rem;
      transition: all 0.2s;
      text-decoration: none;
      display: inline-block;
      font-family: inherit;
      line-height: 1.5;
      min-width: 100px;
    }

    .modal-btn-primary {
      background: linear-gradient(135deg, #c51d34 0%, #a01628 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(197, 29, 52, 0.3);
    }

    .modal-btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(197, 29, 52, 0.4);
      background: linear-gradient(135deg, #a01628 0%, #8b1220 100%);
    }

    .modal-btn-secondary {
      background: #f3f4f6;
      color: #374151;
    }

    .modal-btn-secondary:hover {
      background: #e5e7eb;
    }

    /* Reorder Modal Styles */
    .reorder-modal {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 10000;
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s ease;
    }

    .reorder-modal.show {
      opacity: 1;
      visibility: visible;
    }

    .reorder-modal-content {
      background: white;
      border-radius: 16px;
      padding: 2rem;
      max-width: 600px;
      width: 90%;
      max-height: 80vh;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      transform: scale(0.8);
      transition: transform 0.3s ease;
      display: flex;
      flex-direction: column;
      overflow: hidden;
    }

    .reorder-modal.show .reorder-modal-content {
      transform: scale(1);
    }

    .reorder-modal-header {
      margin-bottom: 1.5rem;
    }

    .reorder-modal-header h2 {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 0.5rem;
    }

    .reorder-modal-header p {
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    .reorder-list {
      flex: 1;
      overflow-y: auto;
      margin-bottom: 1.5rem;
      min-height: 300px;
      max-height: 400px;
    }

    .reorder-item {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 1rem;
      background: var(--bg-secondary);
      border: 2px solid var(--border-primary);
      border-radius: 8px;
      margin-bottom: 0.75rem;
      cursor: move;
      transition: all 0.2s;
      position: relative;
    }

    .reorder-item:hover {
      border-color: var(--accent-red);
      box-shadow: 0 2px 8px rgba(197, 29, 52, 0.1);
    }

    .reorder-item.dragging {
      opacity: 0.5;
      border-color: var(--accent-red);
      box-shadow: 0 4px 12px rgba(197, 29, 52, 0.2);
    }

    .reorder-item.drag-over {
      border-color: var(--accent-red);
      background: rgba(197, 29, 52, 0.05);
    }

    .reorder-handle {
      color: var(--text-secondary);
      font-size: 1.25rem;
      cursor: grab;
    }

    .reorder-handle:active {
      cursor: grabbing;
    }

    .reorder-item-icon {
      width: 48px;
      height: 48px;
      border-radius: 12px;
      background: linear-gradient(135deg, #c51d34 0%, #a01628 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 1.5rem;
      overflow: hidden;
      flex-shrink: 0;
    }

    .reorder-item-icon img {
      width: 100%;
      height: 100%;
      object-fit: contain;
      padding: 4px;
      filter: none !important;
    }

    .reorder-item-info {
      flex: 1;
    }

    .reorder-item-name {
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 0.25rem;
    }

    .reorder-item-description {
      font-size: 0.875rem;
      color: var(--text-secondary);
    }

    .reorder-item-order {
      font-size: 0.875rem;
      color: var(--accent-red);
      font-weight: 600;
      min-width: 40px;
      text-align: center;
    }

    .reorder-modal-actions {
      display: flex;
      gap: 1rem;
      justify-content: flex-end;
      padding-top: 1.5rem;
      border-top: 1px solid var(--border-primary);
    }

    .reorder-modal-actions .btn {
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      border: none;
      font-size: 0.9375rem;
      transition: all 0.2s;
    }

    .reorder-modal-actions .btn-cancel {
      background: var(--bg-secondary);
      color: var(--text-primary);
      border: 1px solid var(--border-primary);
    }

    .reorder-modal-actions .btn-cancel:hover {
      background: #e5e7eb;
    }

    .reorder-modal-actions .btn-save {
      background: linear-gradient(135deg, #c51d34 0%, #a01628 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(197, 29, 52, 0.3);
    }

    .reorder-modal-actions .btn-save:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(197, 29, 52, 0.4);
      background: linear-gradient(135deg, #a01628 0%, #8b1220 100%);
    }

    .reorder-modal-actions .btn-save:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

  </style>
</head>
<body>
  <div class="dashboard-layout">
    <%- include('../partials/admin-sidebar') %>
    
    <div class="dashboard-main">
      <div class="page-header">
        <div>
          <h1>Manage Services</h1>
          <p>Create and manage your services</p>
        </div>
        <div style="display: flex; gap: 1rem; align-items: center; flex-wrap: wrap;">
          <% if (services && services.length > 0) { %>
            <button onclick="openReorderModal()" class="btn-primary" style="background: #6b7280; padding: 0.75rem 1.5rem; border-radius: 8px; border: none; font-weight: 600; cursor: pointer; transition: all 0.2s; text-decoration: none; display: inline-flex; align-items: center; gap: 0.5rem;">
              <i class="fas fa-sort"></i> Reorder Services
            </button>
          <% } %>
          <a href="/admin/services/add" class="btn-primary">
            <i class="fas fa-plus"></i> Add Service
          </a>
        </div>
      </div>

      <!-- Search Bar -->
      <div class="search-container">
        <form method="GET" action="/admin/services" class="search-form">
          <div class="search-input-wrapper">
            <i class="fas fa-search"></i>
            <input 
              type="text" 
              name="search" 
              placeholder="Search by title..." 
              value="<%= filters && filters.search ? filters.search : '' %>"
              class="search-input"
            >
          </div>
          <button type="submit" class="btn-primary">
            <i class="fas fa-search"></i> Search
          </button>
          <% if (filters && filters.search) { %>
            <a href="/admin/services" class="btn btn-secondary" style="text-decoration: none;">
              <i class="fas fa-times"></i> Clear
            </a>
          <% } %>
        </form>
      </div>

      <div class="table-container">
        <% if (services && services.length > 0) { %>
          <!-- Desktop Table View -->
          <div class="table-wrapper">
          <table class="table">
            <thead>
              <tr>
                <th>Title</th>
                <th>Icon</th>
                <th>Projects</th>
                <th>Status</th>
                <th>Order</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% services.forEach(service => { %>
                <tr>
                  <td style="overflow: hidden; text-overflow: ellipsis; white-space: nowrap; max-width: 250px;"><strong><%= service.title %></strong></td>
                  <td>
                    <% if (service.icon) { %>
                      <% if (service.icon.startsWith('/')) { %>
                        <img src="<%= service.icon %>" alt="Service Icon" class="service-icon-img" style="width: 24px; height: 24px; object-fit: contain;" onerror="this.style.display='none'; this.nextElementSibling.style.display='inline';">
                        <i class="fas fa-image" style="display: none;"></i>
                      <% } else { %>
                        <i class="fas fa-<%= service.icon %>"></i>
                      <% } %>
                    <% } %>
                  </td>
                  <td><%= service.project_count || 0 %></td>
                  <td>
                    <span class="status-badge <%= service.is_active ? 'active' : 'inactive' %>">
                      <%= service.is_active ? 'Active' : 'Inactive' %>
                    </span>
                  </td>
                  <td><%= service.display_order || 0 %></td>
                  <td>
                    <div class="action-buttons">
                      <button class="btn btn-success btn-sm btn-icon-only toggle-service" data-service-id="<%= service.id %>" data-status="<%= service.is_active %>" title="<%= service.is_active ? 'Deactivate' : 'Activate' %>">
                        <i class="fas fa-<%= service.is_active ? 'eye-slash' : 'eye' %>"></i>
                      </button>
                      <a href="/admin/services/edit/<%= service.id %>" class="btn btn-secondary btn-sm btn-icon-only" style="text-decoration: none; display: inline-flex;" title="Edit Service">
                        <i class="fas fa-edit"></i>
                      </a>
                      <button class="btn btn-danger btn-sm btn-icon-only delete-service" data-service-id="<%= service.id %>" data-service-title="<%= service.title %>" title="Delete Service">
                        <i class="fas fa-trash"></i>
                      </button>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
          </div>

          <!-- Mobile Card View -->
          <div class="mobile-card-view">
            <% services.forEach(service => { %>
              <div class="mobile-card">
                <div class="mobile-card-header">
                  <div class="mobile-card-icon">
                    <% if (service.icon) { %>
                      <% if (service.icon.startsWith('/')) { %>
                        <img src="<%= service.icon %>" alt="Service Icon" onerror="this.style.display='none'; this.parentElement.innerHTML='<i class=\'fas fa-cog\'></i>';">
                      <% } else { %>
                        <i class="fas fa-<%= service.icon %>"></i>
                      <% } %>
                    <% } else { %>
                      <i class="fas fa-cog"></i>
                    <% } %>
                  </div>
                  <div class="mobile-card-title">
                    <h3><%= service.title %></h3>
                    <p><%= service.project_count || 0 %> project<%= (service.project_count || 0) !== 1 ? 's' : '' %></p>
                  </div>
                  <span class="status-badge <%= service.is_active ? 'active' : 'inactive' %>">
                    <%= service.is_active ? 'Active' : 'Inactive' %>
                  </span>
                </div>
                <div class="mobile-card-body">
                  <div class="mobile-card-row">
                    <span class="mobile-card-label">Order:</span>
                    <span class="mobile-card-value"><%= service.display_order || 0 %></span>
                  </div>
                </div>
                <div class="mobile-card-actions">
                  <button class="btn btn-success toggle-service" data-service-id="<%= service.id %>" data-status="<%= service.is_active %>">
                    <i class="fas fa-<%= service.is_active ? 'eye-slash' : 'eye' %>"></i> <%= service.is_active ? 'Hide' : 'Show' %>
                  </button>
                  <a href="/admin/services/edit/<%= service.id %>" class="btn btn-secondary" style="text-decoration: none;">
                    <i class="fas fa-edit"></i> Edit
                  </a>
                  <button class="btn btn-danger delete-service" data-service-id="<%= service.id %>" data-service-title="<%= service.title %>">
                    <i class="fas fa-trash"></i>
                  </button>
                </div>
              </div>
            <% }) %>
          </div>
        <% } else { %>
          <div class="empty-state">
            <i class="fas fa-cogs"></i>
            <h3>No Services Found</h3>
          </div>
        <% } %>
      </div>

      <!-- Pagination -->
      <% if (pagination && pagination.totalPages > 1) { %>
        <div class="pagination-container">
          <div class="pagination-info">
            Showing <%= ((pagination.currentPage - 1) * pagination.limit) + 1 %> to <%= Math.min(pagination.currentPage * pagination.limit, pagination.totalCount) %> of <%= pagination.totalCount %> results
          </div>
          <div class="pagination">
            <% 
              const buildQuery = (page) => {
                const params = [];
                if (filters.search) params.push(`search=${encodeURIComponent(filters.search)}`);
                params.push(`page=${page}`);
                return params.length > 0 ? '?' + params.join('&') : '';
              };
            %>
            <% if (pagination.hasPrev) { %>
              <a href="/admin/services<%= buildQuery(pagination.currentPage - 1) %>" class="pagination-btn">
                <i class="fas fa-chevron-left"></i>
              </a>
            <% } else { %>
              <span class="pagination-btn disabled">
                <i class="fas fa-chevron-left"></i>
              </span>
            <% } %>

            <% 
              let startPage = Math.max(1, pagination.currentPage - 2);
              let endPage = Math.min(pagination.totalPages, pagination.currentPage + 2);
              if (endPage - startPage < 4) {
                if (startPage === 1) {
                  endPage = Math.min(5, pagination.totalPages);
                } else {
                  startPage = Math.max(1, pagination.totalPages - 4);
                }
              }
            %>

            <% if (startPage > 1) { %>
              <a href="/admin/services<%= buildQuery(1) %>" class="pagination-btn">1</a>
              <% if (startPage > 2) { %>
                <span class="pagination-ellipsis">...</span>
              <% } %>
            <% } %>

            <% for (let i = startPage; i <= endPage; i++) { %>
              <% if (i === pagination.currentPage) { %>
                <span class="pagination-btn active"><%= i %></span>
              <% } else { %>
                <a href="/admin/services<%= buildQuery(i) %>" class="pagination-btn"><%= i %></a>
              <% } %>
            <% } %>

            <% if (endPage < pagination.totalPages) { %>
              <% if (endPage < pagination.totalPages - 1) { %>
                <span class="pagination-ellipsis">...</span>
              <% } %>
              <a href="/admin/services<%= buildQuery(pagination.totalPages) %>" class="pagination-btn"><%= pagination.totalPages %></a>
            <% } %>

            <% if (pagination.hasNext) { %>
              <a href="/admin/services<%= buildQuery(pagination.currentPage + 1) %>" class="pagination-btn">
                <i class="fas fa-chevron-right"></i>
              </a>
            <% } else { %>
              <span class="pagination-btn disabled">
                <i class="fas fa-chevron-right"></i>
              </span>
            <% } %>
          </div>
        </div>
      <% } %>
    </div>
  </div>

  <script>

    function showModal(modalId) {
      const modal = document.getElementById(modalId);
      if (modal) {
        modal.classList.add('show');
      }
    }

    function closeModal(modalId) {
      const modal = document.getElementById(modalId);
      if (modal) {
        modal.classList.remove('show');
      }
    }


    function showConfirmation(message, onConfirm) {
      const modal = document.getElementById('confirmationModal');
      const messageEl = document.getElementById('confirmationMessage');
      const confirmBtn = document.getElementById('confirmBtn');
      
      if (modal && messageEl) {
        messageEl.textContent = message;
        modal.classList.add('show');
        

        const newConfirmBtn = confirmBtn.cloneNode(true);
        confirmBtn.parentNode.replaceChild(newConfirmBtn, confirmBtn);
        
        newConfirmBtn.onclick = function() {
          closeModal('confirmationModal');
          if (onConfirm) onConfirm();
        };
      }
    }


    document.querySelectorAll('.toggle-service').forEach(btn => {
      btn.addEventListener('click', async function() {
        const serviceId = this.dataset.serviceId;
        const currentStatus = this.dataset.status === 'true';

        try {
          const response = await fetch(`/admin/services/${serviceId}/toggle`, {
            method: 'POST'
          });

          const data = await response.json();

          if (data.success) {
            location.reload();
          } else {
            document.getElementById('errorMessage').textContent = data.message || 'Error toggling service status';
            showModal('errorModal');
          }
        } catch (error) {
          console.error('Error:', error);
          document.getElementById('errorMessage').textContent = 'An error occurred while toggling service status';
          showModal('errorModal');
        }
      });
    });


    document.querySelectorAll('.delete-service').forEach(btn => {
      btn.addEventListener('click', async function() {
        const serviceId = this.dataset.serviceId;
        const serviceTitle = this.dataset.serviceTitle;

        showConfirmation(`Are you sure you want to delete service "${serviceTitle}"? This action cannot be undone.`, async function() {
          try {
            const response = await fetch(`/admin/services/${serviceId}`, {
              method: 'DELETE'
            });

            const data = await response.json();

            if (data.success) {
              document.getElementById('successMessage').textContent = 'Service deleted successfully';
              showModal('successModal');
              setTimeout(() => {
                location.reload();
              }, 1500);
            } else {
              document.getElementById('errorMessage').textContent = data.message || 'Error deleting service';
              showModal('errorModal');
            }
          } catch (error) {
            console.error('Error:', error);
            document.getElementById('errorMessage').textContent = 'An error occurred while deleting the service';
            showModal('errorModal');
          }
        });
      });
    });


    document.addEventListener('click', function(e) {
      if (e.target.classList.contains('success-modal') || e.target.classList.contains('confirmation-modal')) {
        closeModal(e.target.id);
      }
    });
  </script>

  <!-- Success Modal -->
  <div class="success-modal" id="successModal">
    <div class="modal-content-wrapper">
      <div class="modal-icon">
        <i class="fas fa-check"></i>
      </div>
      <h3 class="modal-title">Success!</h3>
      <p class="modal-message" id="successMessage">Operation completed successfully.</p>
      <div class="modal-actions">
        <button onclick="closeModal('successModal')" class="modal-btn modal-btn-primary">OK</button>
      </div>
    </div>
  </div>

  <!-- Error Modal -->
  <div class="success-modal" id="errorModal">
    <div class="modal-content-wrapper">
      <div class="modal-icon error">
        <i class="fas fa-exclamation-triangle"></i>
      </div>
      <h3 class="modal-title">Error Occurred</h3>
      <p class="modal-message" id="errorMessage">Something went wrong. Please try again.</p>
      <div class="modal-actions">
        <button onclick="closeModal('errorModal')" class="modal-btn modal-btn-primary">Close</button>
      </div>
    </div>
  </div>

  <!-- Confirmation Modal -->
  <div class="confirmation-modal" id="confirmationModal">
    <div class="modal-content-wrapper">
      <div class="modal-icon warning">
        <i class="fas fa-question-circle"></i>
      </div>
      <h3 class="modal-title">Confirm Action</h3>
      <p class="modal-message" id="confirmationMessage">Are you sure you want to proceed?</p>
      <div class="modal-actions">
        <button onclick="closeModal('confirmationModal')" class="modal-btn modal-btn-secondary">Cancel</button>
        <button id="confirmBtn" class="modal-btn modal-btn-primary">Confirm</button>
      </div>
    </div>
  </div>

  <!-- Reorder Modal -->
  <div class="reorder-modal" id="reorderModal">
    <div class="reorder-modal-content">
      <div class="reorder-modal-header">
        <h2>Reorder Services</h2>
        <p>Drag and drop services to reorder them. The topmost service will have order 0.</p>
      </div>
      <div class="reorder-list" id="reorderList">
        <!-- Services will be populated here -->
      </div>
      <div class="reorder-modal-actions">
        <button class="btn btn-cancel" onclick="closeReorderModal()">Cancel</button>
        <button class="btn btn-save" id="saveReorderBtn" onclick="saveReorder()">Save Order</button>
      </div>
    </div>
  </div>

  <!-- Reorder Success Modal -->
  <div class="reorder-modal" id="reorderSuccessModal">
    <div class="reorder-modal-content" style="max-width: 400px;">
      <div style="text-align: center;">
        <div style="width: 80px; height: 80px; background: linear-gradient(135deg, #10b981 0%, #059669 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 1.5rem;">
          <i class="fas fa-check" style="color: white; font-size: 2.5rem;"></i>
        </div>
        <h2 style="font-size: 1.5rem; font-weight: 700; color: var(--text-primary); margin-bottom: 0.5rem;">Success!</h2>
        <p style="color: var(--text-secondary); margin-bottom: 1.5rem;" id="reorderSuccessMessage">Service order updated successfully.</p>
        <button class="btn btn-save" onclick="closeReorderSuccessModal()">OK</button>
      </div>
    </div>
  </div>

  <!-- Reorder Error Modal -->
  <div class="reorder-modal" id="reorderErrorModal">
    <div class="reorder-modal-content" style="max-width: 400px;">
      <div style="text-align: center;">
        <div style="width: 80px; height: 80px; background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 1.5rem;">
          <i class="fas fa-exclamation-triangle" style="color: white; font-size: 2.5rem;"></i>
        </div>
        <h2 style="font-size: 1.5rem; font-weight: 700; color: var(--text-primary); margin-bottom: 0.5rem;">Error</h2>
        <p style="color: var(--text-secondary); margin-bottom: 1.5rem;" id="reorderErrorMessage">Something went wrong. Please try again.</p>
        <button class="btn btn-save" onclick="closeReorderErrorModal()">Close</button>
      </div>
    </div>
  </div>

  <script>
    let services = <%- JSON.stringify(services || []) %>;
    let reorderedServices = [];

    function openReorderModal() {
      reorderedServices = [...services];
      renderReorderList();
      document.getElementById('reorderModal').classList.add('show');

      setTimeout(processAllImages, 200);
    }

    function closeReorderModal() {
      document.getElementById('reorderModal').classList.remove('show');
      reorderedServices = [...services];
    }

    function closeReorderSuccessModal() {
      document.getElementById('reorderSuccessModal').classList.remove('show');
      closeReorderModal();
      location.reload();
    }

    function closeReorderErrorModal() {
      document.getElementById('reorderErrorModal').classList.remove('show');
    }

    function renderReorderList() {
      const list = document.getElementById('reorderList');
      list.innerHTML = '';

      reorderedServices.forEach((service, index) => {
        const item = document.createElement('div');
        item.className = 'reorder-item';
        item.draggable = true;
        item.dataset.index = index;
        item.dataset.id = service.id;


        let iconHtml = '';
        if (service.icon) {
          if (service.icon.startsWith('/')) {
            iconHtml = `<img src="${service.icon}" alt="Service Icon" class="service-icon-img" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"><i class="fas fa-cog" style="display: none;"></i>`;
          } else {
            iconHtml = `<i class="fas fa-${service.icon}"></i>`;
          }
        } else {
          iconHtml = '<i class="fas fa-cog"></i>';
        }

        item.innerHTML = `
          <div class="reorder-handle">
            <i class="fas fa-grip-vertical"></i>
          </div>
          <div class="reorder-item-icon">
            ${iconHtml}
          </div>
          <div class="reorder-item-info">
            <div class="reorder-item-name">${service.title || 'Untitled Service'}</div>
            <div class="reorder-item-description">${service.short_description || 'No description'}</div>
          </div>
          <div class="reorder-item-order">${index}</div>
        `;


        item.addEventListener('dragstart', handleDragStart);
        item.addEventListener('dragend', handleDragEnd);
        item.addEventListener('dragover', handleDragOver);
        item.addEventListener('drop', handleDrop);
        item.addEventListener('dragenter', handleDragEnter);
        item.addEventListener('dragleave', handleDragLeave);

        list.appendChild(item);
      });
    }

    let draggedElement = null;
    let draggedIndex = null;

    function handleDragStart(e) {
      draggedElement = this;
      draggedIndex = parseInt(this.dataset.index);
      this.classList.add('dragging');
      e.dataTransfer.effectAllowed = 'move';
      e.dataTransfer.setData('text/html', this.innerHTML);
    }

    function handleDragEnd(e) {
      this.classList.remove('dragging');
      document.querySelectorAll('.reorder-item').forEach(item => {
        item.classList.remove('drag-over');
      });
    }

    function handleDragOver(e) {
      if (e.preventDefault) {
        e.preventDefault();
      }
      e.dataTransfer.dropEffect = 'move';
      return false;
    }

    function handleDragEnter(e) {
      if (this !== draggedElement) {
        this.classList.add('drag-over');
      }
    }

    function handleDragLeave(e) {
      this.classList.remove('drag-over');
    }

    function handleDrop(e) {
      if (e.stopPropagation) {
        e.stopPropagation();
      }

      if (draggedElement !== this) {
        const dropIndex = parseInt(this.dataset.index);
        const draggedService = reorderedServices[draggedIndex];
        

        reorderedServices.splice(draggedIndex, 1);

        reorderedServices.splice(dropIndex, 0, draggedService);
        

        renderReorderList();

        setTimeout(processAllImages, 100);
      }

      this.classList.remove('drag-over');
      return false;
    }

    async function saveReorder() {
      const saveBtn = document.getElementById('saveReorderBtn');
      saveBtn.disabled = true;
      saveBtn.textContent = 'Saving...';

      try {
        const serviceIds = reorderedServices.map(service => service.id);
        
        const response = await fetch('/admin/services/reorder', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({ serviceIds })
        });


        const contentType = response.headers.get('content-type');
        if (!contentType || !contentType.includes('application/json')) {
          const text = await response.text();
          console.error('Non-JSON response:', text.substring(0, 200));
          throw new Error('Server returned non-JSON response. Please check the console for details.');
        }

        const data = await response.json();

        if (data.success) {
          document.getElementById('reorderSuccessMessage').textContent = data.message || 'Service order updated successfully.';
          document.getElementById('reorderModal').classList.remove('show');
          document.getElementById('reorderSuccessModal').classList.add('show');
        } else {
          document.getElementById('reorderErrorMessage').textContent = data.message || 'Error updating service order.';
          document.getElementById('reorderErrorModal').classList.add('show');
          saveBtn.disabled = false;
          saveBtn.textContent = 'Save Order';
        }
      } catch (error) {
        console.error('Error:', error);
        document.getElementById('reorderErrorMessage').textContent = error.message || 'An error occurred while updating the order.';
        document.getElementById('reorderErrorModal').classList.add('show');
        saveBtn.disabled = false;
        saveBtn.textContent = 'Save Order';
      }
    }


    document.addEventListener('click', function(e) {
      if (e.target.classList.contains('reorder-modal')) {
        if (e.target.id === 'reorderModal') {
          closeReorderModal();
        } else if (e.target.id === 'reorderSuccessModal') {
          closeReorderSuccessModal();
        } else if (e.target.id === 'reorderErrorModal') {
          closeReorderErrorModal();
        }
      }
    });
  </script>

  <script>
    function checkImageBrightness(img) {
      return new Promise((resolve) => {

        if (img.dataset.processed === 'true') {
          resolve(false);
          return;
        }
        

        if (!img.complete || img.naturalWidth === 0 || img.naturalHeight === 0) {
          const checkLoaded = () => {
            if (img.complete && img.naturalWidth > 0 && img.naturalHeight > 0) {
              analyzeImage(img).then(resolve);
            } else {
              setTimeout(checkLoaded, 100);
            }
          };
          checkLoaded();
          return;
        }
        
        analyzeImage(img).then(resolve);
      });
    }
    
    function analyzeImage(img) {
      return new Promise((resolve) => {
        try {
          const canvas = document.createElement('canvas');
          const ctx = canvas.getContext('2d');
          canvas.width = img.naturalWidth || img.width;
          canvas.height = img.naturalHeight || img.height;
          
          if (canvas.width === 0 || canvas.height === 0) {
            img.dataset.processed = 'true';
            resolve(false);
            return;
          }
          
          ctx.drawImage(img, 0, 0);
          

          const samplePoints = [];
          const gridSize = 5; 
          
          for (let i = 0; i <= gridSize; i++) {
            for (let j = 0; j <= gridSize; j++) {
              samplePoints.push({
                x: (i / gridSize) * canvas.width,
                y: (j / gridSize) * canvas.height
              });
            }
          }
          
          let totalBrightness = 0;
          let sampleCount = 0;
          let whitePixelCount = 0;
          
          samplePoints.forEach(point => {
            const x = Math.floor(point.x);
            const y = Math.floor(point.y);
            
            if (x >= 0 && x < canvas.width && y >= 0 && y < canvas.height) {
              try {
                const pixelData = ctx.getImageData(x, y, 1, 1).data;
                const r = pixelData[0];
                const g = pixelData[1];
                const b = pixelData[2];
                const a = pixelData[3];
                

                if (a < 128) return;
                

                const brightness = (r + g + b) / 3;
                totalBrightness += brightness;
                sampleCount++;
                

                if (brightness > 200) {
                  whitePixelCount++;
                }
              } catch (e) {

              }
            }
          });
          
          if (sampleCount === 0) {
            img.dataset.processed = 'true';
            resolve(false);
            return;
          }
          
          const averageBrightness = totalBrightness / sampleCount;
          const whitePixelRatio = whitePixelCount / sampleCount;
          



          const shouldConvert = averageBrightness > 200 || whitePixelRatio > 0.8;
          

          const isInModal = img.closest('.reorder-modal');
          
          if (shouldConvert && !isInModal) {
            img.style.filter = 'brightness(0)';
            img.style.transition = 'filter 0.3s ease';
          }
          
          img.dataset.processed = 'true';
          resolve(shouldConvert && !isInModal);
        } catch (e) {
          console.warn('Error analyzing image:', e);
          img.dataset.processed = 'true';
          resolve(false);
        }
      });
    }
    
    function processAllImages() {

      const serviceImages = document.querySelectorAll('.service-icon-img:not([data-processed="true"])');
      
      if (serviceImages.length === 0) return;
      
      serviceImages.forEach(img => {

        if (!img.closest('.reorder-modal')) {
          checkImageBrightness(img);
        }
      });
    }
    

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', function() {

        setTimeout(processAllImages, 100);
        

        setTimeout(processAllImages, 1000);
        setTimeout(processAllImages, 2000);
        

        const observer = new MutationObserver(processAllImages);
        observer.observe(document.body, {
          childList: true,
          subtree: true
        });
      });
    } else {

      setTimeout(processAllImages, 100);
      setTimeout(processAllImages, 1000);
      setTimeout(processAllImages, 2000);
    }
    

    document.addEventListener('load', function(e) {
      if (e.target.classList && e.target.classList.contains('service-icon-img')) {
        checkImageBrightness(e.target);
      }
    }, true);
  </script>
</body>
</html>

