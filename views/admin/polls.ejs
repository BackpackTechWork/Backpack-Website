<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      margin: 0;
      padding: 0;
      margin-top: 0;
      padding-top: 0;
    }

    :root {
      --bg-primary: #ffffff;
      --bg-secondary: #f9fafb;
      --border-primary: #e5e7eb;
      --text-primary: #111827;
      --text-secondary: #6b7280;
      --accent-red: #c51d34;
    }

    .dashboard-layout {
      display: flex;
      min-height: 100vh;
      margin: 0;
      padding: 0;
      margin-top: 0;
      padding-top: 0;
    }

    .dashboard-main {
      flex: 1;
      padding: 0.25rem 2.5rem 2rem 2.5rem;
      margin-left: 280px;
      background: var(--bg-secondary);
      width: calc(100% - 280px);
      min-width: 0;
      margin-top: 0;
      padding-top: 0.25rem;
    }

    .page-header {
      margin-bottom: 2rem;
      margin-top: 0;
      padding-top: 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .page-header h1 {
      font-size: 2rem;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 0.5rem;
      line-height: 1.2;
    }

    .page-header p {
      color: var(--text-secondary);
      font-size: 0.9375rem;
      margin: 0;
    }

    .btn-primary {
      background: var(--accent-red);
      color: white;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      border: none;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      white-space: nowrap;
      touch-action: manipulation;
    }

    .btn-primary:hover {
      background: #a01628;
    }

    .tabs-container {
      background: var(--bg-primary);
      border-radius: 12px;
      box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.05);
      padding: 0.5rem;
      margin-bottom: 1.5rem;
      display: flex;
      gap: 0.5rem;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
    }

    .tab-link {
      flex: 1;
      padding: 0.75rem 1.5rem;
      text-align: center;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      color: var(--text-primary);
      background: var(--bg-secondary);
      transition: all 0.2s;
      white-space: nowrap;
      min-width: 120px;
      touch-action: manipulation;
    }

    .tab-link.active {
      background: var(--accent-red);
      color: white;
    }

    .tab-link:hover {
      background: var(--accent-red);
      color: white;
    }

    .table-container {
      background: var(--bg-primary);
      border-radius: 12px;
      box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.05);
      overflow: hidden;
      margin-bottom: 2rem;
      width: 100%;
      max-width: 100%;
    }

    .table {
      width: 100%;
      border-collapse: collapse;
      table-layout: auto;
    }

    .table-wrapper {
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
    }

    .table thead {
      background: var(--bg-secondary);
    }

    .table th {
      padding: 1rem 1.5rem;
      text-align: left;
      font-weight: 600;
      font-size: 0.875rem;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .table td {
      padding: 1rem 1.5rem;
      border-top: 1px solid var(--border-primary);
      color: var(--text-primary);
    }

    .table tbody tr:hover {
      background: var(--bg-secondary);
    }

    .status-badge {
      display: inline-block;
      padding: 0.25rem 0.75rem;
      border-radius: 6px;
      font-size: 0.8125rem;
      font-weight: 600;
    }

    .status-badge.active {
      background: rgba(34, 197, 94, 0.1);
      color: #22c55e;
    }

    .status-badge.inactive {
      background: rgba(107, 114, 128, 0.1);
      color: #6b7280;
    }

    .btn {
      padding: 0.5rem 1rem;
      border-radius: 6px;
      font-size: 0.875rem;
      font-weight: 500;
      cursor: pointer;
      border: none;
      transition: all 0.2s;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      touch-action: manipulation;
      -webkit-tap-highlight-color: transparent;
    }

    .btn-sm {
      padding: 0.5rem;
      font-size: 0.875rem;
      width: 36px;
      height: 36px;
      justify-content: center;
    }

    .btn-icon-only {
      padding: 0.5rem;
      width: 36px;
      height: 36px;
      gap: 0;
    }

    .btn-danger {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
    }

    .btn-danger:hover {
      background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(239, 68, 68, 0.4);
    }

    .btn-secondary {
      background: linear-gradient(135deg, #6b7280 0%, #4b5563 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(107, 114, 128, 0.3);
    }

    .btn-secondary:hover {
      background: linear-gradient(135deg, #4b5563 0%, #374151 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(107, 114, 128, 0.4);
    }

    .action-buttons {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }

    .action-buttons .btn {
      min-width: 36px;
      min-height: 36px;
      touch-action: manipulation;
    }

    .empty-state {
      padding: 4rem 2rem;
      text-align: center;
      color: var(--text-secondary);
    }

    .empty-state i {
      font-size: 4rem;
      margin-bottom: 1.5rem;
      opacity: 0.3;
      color: var(--text-secondary);
    }

    .empty-state h3 {
      font-size: 1.25rem;
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 0.5rem;
    }

    .empty-state p {
      font-size: 0.9375rem;
      margin: 0;
      color: var(--text-secondary);
    }

    .pagination-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 1.5rem;
      padding: 1rem 0;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .pagination-info {
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    .pagination {
      display: flex;
      gap: 0.5rem;
      align-items: center;
      flex-wrap: wrap;
    }

    .pagination-btn {
      padding: 0.5rem 0.75rem;
      border-radius: 6px;
      background: var(--bg-primary);
      border: 1px solid var(--border-primary);
      color: var(--text-primary);
      text-decoration: none;
      font-size: 0.875rem;
      font-weight: 500;
      transition: all 0.2s;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      cursor: pointer;
    }

    .pagination-btn:hover:not(.disabled):not(.active) {
      background: var(--bg-secondary);
      border-color: var(--accent-red);
      color: var(--accent-red);
    }

    .pagination-btn.active {
      background: var(--accent-red);
      color: white;
      border-color: var(--accent-red);
    }

    .pagination-btn.disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

    .pagination-ellipsis {
      padding: 0.5rem 0.25rem;
      color: var(--text-secondary);
    }

    @media (max-width: 768px) {
      .pagination-container {
        flex-direction: column;
        align-items: stretch;
      }

      .pagination-info {
        text-align: center;
      }

      .pagination {
        justify-content: center;
      }
    }

    /* Mobile Card Layout */
    .mobile-card-view {
      display: none;
      padding: 0.5rem;
    }

    .mobile-card {
      background: var(--bg-primary);
      border: 1px solid var(--border-primary);
      border-radius: 12px;
      padding: 1rem;
      margin-bottom: 0.75rem;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
      transition: all 0.2s ease;
    }

    .mobile-card:hover {
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
    }

    .mobile-card-header {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 0.75rem;
      margin-bottom: 0.75rem;
      padding-bottom: 0.75rem;
      border-bottom: 1px solid var(--border-primary);
      flex-wrap: wrap;
    }

    .mobile-card-title {
      flex: 1;
      min-width: 0;
    }

    .mobile-card-title h3 {
      margin: 0 0 0.5rem 0;
      font-size: 0.9375rem;
      font-weight: 700;
      color: var(--text-primary);
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
      word-break: break-word;
      line-height: 1.3;
    }

    .mobile-card-title p {
      margin: 0;
      font-size: 0.8125rem;
      color: var(--text-secondary);
    }

    .mobile-card-body {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .mobile-card-row {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 0.75rem;
      font-size: 0.8125rem;
    }

    .mobile-card-label {
      color: var(--text-secondary);
      font-weight: 600;
      min-width: 60px;
      flex-shrink: 0;
      font-size: 0.8125rem;
    }

    .mobile-card-value {
      color: var(--text-primary);
      text-align: right;
      flex: 1;
      word-break: break-word;
      font-size: 0.8125rem;
    }

    .mobile-card-votes {
      display: flex;
      align-items: center;
      justify-content: flex-end;
      gap: 0.5rem;
      font-weight: 700;
      color: var(--accent-red);
    }

    .mobile-card-votes i {
      font-size: 1rem;
    }

    .mobile-card-actions {
      margin-top: 0.75rem;
      padding-top: 0.75rem;
      border-top: 1px solid var(--border-primary);
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }

    .mobile-card-actions .btn {
      flex: 1;
      justify-content: center;
      min-height: 44px;
      font-size: 0.8125rem;
      padding: 0.625rem 0.75rem;
      min-width: 0;
    }

    .mobile-card-actions .btn i {
      font-size: 0.875rem;
    }

    /* Desktop large screen adjustments */
    @media (min-width: 1400px) {
      .dashboard-main {
        padding: 2.5rem 4rem;
      }

      .table th,
      .table td {
        padding: 1.25rem 2rem;
      }
    }

    @media (min-width: 1600px) {
      .dashboard-main {
        padding: 3rem 5rem;
      }
    }

    /* Tablet landscape: 1024px - 1199px */
    @media (min-width: 1024px) and (max-width: 1199px) {
      .dashboard-main {
        padding: 1.5rem 2rem;
        margin-left: 260px;
        width: calc(100% - 260px);
      }

      .page-header h1 {
        font-size: 1.75rem;
      }

      .table th,
      .table td {
        padding: 0.875rem 1.25rem;
        font-size: 0.875rem;
      }
    }

    /* Large desktop: 1200px - 1400px */
    @media (min-width: 1200px) and (max-width: 1399px) {
      .dashboard-main {
        padding: 2rem 3rem;
        margin-left: 280px;
        width: calc(100% - 280px);
      }

      .page-header h1 {
        font-size: 1.875rem;
      }

      .table th,
      .table td {
        padding: 1rem 1.5rem;
      }
    }

    /* Tablet portrait: 769px - 1023px */
    @media (max-width: 1023px) and (min-width: 769px) {
      .dashboard-main {
        padding: 1.5rem;
        margin-left: 0;
        width: 100%;
      }

      .page-header h1 {
        font-size: 1.75rem;
      }

      .page-header p {
        font-size: 0.875rem;
      }

      .table-wrapper {
        overflow-x: auto;
      }

      .table th,
      .table td {
        padding: 0.75rem 1rem;
        font-size: 0.8125rem;
        white-space: nowrap;
      }

      .table td:first-child {
        white-space: normal;
        max-width: 200px;
      }
    }

    /* Desktop: Show table, hide cards */
    @media (min-width: 769px) {
      .table-container .table {
        display: table;
      }

      .mobile-card-view {
        display: none !important;
      }
    }

    /* Mobile landscape: 481px - 768px */
    @media (max-width: 768px) and (min-width: 481px) {
      .dashboard-main {
        margin-left: 0 !important;
        padding: 1rem 1rem 1rem 1rem !important;
        padding-top: 1rem !important;
        width: 100%;
      }

      .page-header {
        margin-bottom: 1.5rem;
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }

      .page-header > div:last-child {
        width: 100%;
      }

      .page-header h1 {
        font-size: 1.75rem;
        margin-bottom: 0.25rem;
      }

      .page-header p {
        font-size: 1rem;
      }

      .btn-primary {
        width: 100%;
        justify-content: center;
        padding: 0.75rem 1rem;
      }

      .table-container .table {
        display: none !important;
      }

      .mobile-card-view {
        display: block !important;
        padding: 0.25rem;
      }

      .mobile-card {
        padding: 0.875rem;
      }

      .modal-content-wrapper {
        padding: 1.5rem;
        width: 95%;
        max-height: 85vh;
      }

      .modal-content {
        padding: 1.5rem;
        width: 95%;
      }

      .modal-title {
        font-size: 1.5rem;
      }

      .modal-message {
        font-size: 0.9375rem;
      }

      .modal-actions {
        flex-direction: column;
        gap: 0.75rem;
      }

      .modal-btn {
        width: 100%;
        padding: 0.875rem 1.5rem;
      }
    }

    /* Mobile portrait: up to 480px */
    @media (max-width: 480px) {
      .dashboard-main {
        margin-left: 0 !important;
        padding: 1rem 0.75rem 0.75rem 0.75rem !important;
        padding-top: 1rem !important;
        width: 100%;
      }

      .page-header {
        margin-bottom: 1rem;
        flex-direction: column;
        align-items: flex-start;
        gap: 0.75rem;
      }

      .page-header > div:last-child {
        width: 100%;
      }

      .page-header h1 {
        font-size: 1.5rem;
        margin-bottom: 0.25rem;
        line-height: 1.3;
      }

      .page-header p {
        font-size: 0.875rem;
      }

      .btn-primary {
        width: 100%;
        justify-content: center;
        padding: 0.75rem 1rem;
        font-size: 0.875rem;
      }

      .btn-primary i {
        font-size: 0.875rem;
      }

      .table-container {
        border-radius: 8px;
      }

      .table-container .table {
        display: none !important;
      }

      .mobile-card-view {
        display: block !important;
        padding: 0;
      }

      .mobile-card {
        padding: 0.75rem;
        margin-bottom: 0.625rem;
        border-radius: 8px;
      }

      .mobile-card-header {
        gap: 0.625rem;
        margin-bottom: 0.625rem;
        padding-bottom: 0.625rem;
      }

      .mobile-card-title h3 {
        font-size: 0.875rem;
      }

      .mobile-card-title p {
        font-size: 0.75rem;
      }

      .mobile-card-body {
        gap: 0.625rem;
      }

      .mobile-card-row {
        font-size: 0.75rem;
        gap: 0.5rem;
      }

      .mobile-card-label {
        min-width: 55px;
        font-size: 0.75rem;
      }

      .mobile-card-value {
        font-size: 0.75rem;
      }

      .status-badge {
        font-size: 0.75rem;
        padding: 0.25rem 0.625rem;
      }

      .mobile-card-votes {
        font-size: 0.75rem;
      }

      .mobile-card-votes i {
        font-size: 0.875rem;
      }

      .mobile-card-actions {
        margin-top: 0.625rem;
        padding-top: 0.625rem;
        gap: 0.375rem;
        flex-direction: column;
      }

      .mobile-card-actions .btn {
        width: 100%;
        min-height: 42px;
        font-size: 0.8125rem;
        padding: 0.625rem;
      }

      .mobile-card-actions .btn i {
        font-size: 0.8125rem;
      }

      .tabs-container {
        padding: 0.375rem;
        margin-bottom: 1rem;
        border-radius: 8px;
      }

      .tab-link {
        padding: 0.625rem 1rem;
        font-size: 0.8125rem;
        min-width: 100px;
      }

      .empty-state {
        padding: 2rem 1rem;
      }

      .empty-state i {
        font-size: 2.5rem;
        margin-bottom: 0.75rem;
      }

      .empty-state h3 {
        font-size: 1.125rem;
      }

      .empty-state p {
        font-size: 0.875rem;
      }

      .modal-content-wrapper {
        padding: 1.25rem;
        width: 95%;
        max-height: 90vh;
        border-radius: 12px;
      }

      .modal-content {
        padding: 1.25rem;
        width: 95%;
        border-radius: 12px;
      }

      .modal-header {
        margin-bottom: 1rem;
        padding-bottom: 0.75rem;
      }

      .modal-header h2 {
        font-size: 1.25rem;
      }

      .modal-icon {
        width: 64px;
        height: 64px;
        margin-bottom: 1rem;
      }

      .modal-icon i {
        font-size: 2rem;
      }

      .modal-title {
        font-size: 1.25rem;
        margin-bottom: 0.5rem;
      }

      .modal-message {
        font-size: 0.875rem;
        margin-bottom: 1.5rem;
        line-height: 1.5;
      }

      .modal-actions {
        flex-direction: column;
        gap: 0.625rem;
      }

      .modal-btn {
        width: 100%;
        padding: 0.875rem 1.25rem;
        font-size: 0.875rem;
      }

      .poll-results {
        max-width: 100%;
      }

      .poll-result-header {
        font-size: 0.8125rem;
      }

      .poll-result-label {
        font-size: 0.75rem;
      }

      .poll-result-stats {
        font-size: 0.75rem;
      }
    }

    /* Extra small devices: up to 360px */
    @media (max-width: 360px) {
      .dashboard-main {
        padding: 0.5rem;
      }

      .page-header h1 {
        font-size: 1.125rem;
      }

      .mobile-card {
        padding: 0.625rem;
      }

      .mobile-card-title h3 {
        font-size: 0.8125rem;
      }

      .mobile-card-title p {
        font-size: 0.6875rem;
      }

      .mobile-card-row {
        font-size: 0.6875rem;
      }

      .mobile-card-label {
        min-width: 50px;
        font-size: 0.6875rem;
      }

      .mobile-card-value {
        font-size: 0.6875rem;
      }
    }

    .poll-results {
      max-width: 400px;
    }

    .poll-result-item {
      margin-bottom: 0.75rem;
    }

    .poll-result-item:last-child {
      margin-bottom: 0;
    }

    .poll-result-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.25rem;
      font-size: 0.875rem;
    }

    .poll-result-label {
      color: var(--text-primary);
      font-weight: 500;
      flex: 1;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      margin-right: 0.5rem;
    }

    .poll-result-stats {
      color: var(--text-secondary);
      font-size: 0.8125rem;
      white-space: nowrap;
    }

    .poll-result-bar {
      height: 8px;
      background: var(--bg-secondary);
      border-radius: 4px;
      overflow: hidden;
      position: relative;
    }

    .poll-result-fill {
      height: 100%;
      background: linear-gradient(90deg, var(--accent-red) 0%, #e63946 100%);
      border-radius: 4px;
      transition: width 0.3s ease;
    }

    .poll-result-fill.no-votes {
      background: var(--border-primary);
    }

    .btn-info {
      background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
    }

    .btn-info:hover {
      background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(59, 130, 246, 0.4);
    }

    /* Modal Styles */
    .modal-overlay {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.5);
      z-index: 1000;
      align-items: center;
      justify-content: center;
      animation: fadeIn 0.2s ease;
    }

    .modal-overlay.active {
      display: flex;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    .modal-content {
      background: var(--bg-primary);
      border-radius: 12px;
      padding: 2rem;
      max-width: 600px;
      width: 90%;
      max-height: 90vh;
      overflow-y: auto;
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
      animation: slideUp 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    @keyframes slideUp {
      from {
        transform: translateY(20px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    .modal-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1.5rem;
      padding-bottom: 1rem;
      border-bottom: 1px solid var(--border-primary);
    }

    .modal-header h2 {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--text-primary);
      margin: 0;
    }

    .modal-close {
      background: none;
      border: none;
      font-size: 1.5rem;
      color: var(--text-secondary);
      cursor: pointer;
      padding: 0.5rem;
      line-height: 1;
      transition: color 0.2s;
    }

    .modal-close:hover {
      color: var(--text-primary);
    }

    .modal-results {
      margin-top: 1rem;
    }

    .modal-results .poll-results {
      max-width: 100%;
    }

    .modal-results .poll-result-item {
      margin-bottom: 0.75rem;
    }

    .modal-results .poll-result-item:last-child {
      margin-bottom: 0;
    }

    .modal-results .poll-result-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.25rem;
      font-size: 0.875rem;
    }

    .modal-results .poll-result-label {
      color: var(--text-primary);
      font-weight: 500;
      flex: 1;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      margin-right: 0.5rem;
    }

    .modal-results .poll-result-stats {
      color: var(--text-secondary);
      font-size: 0.8125rem;
      white-space: nowrap;
    }

    .modal-results .poll-result-bar {
      height: 8px;
      background: var(--bg-secondary);
      border-radius: 4px;
      overflow: hidden;
      position: relative;
    }

    .modal-results .poll-result-fill {
      height: 100%;
      background: linear-gradient(90deg, var(--accent-red) 0%, #e63946 100%);
      border-radius: 4px;
      transition: width 0.3s ease;
    }

    .modal-results .poll-result-fill.no-votes {
      background: var(--border-primary);
    }

    .modal-total {
      margin-top: 1.5rem;
      padding-top: 1.5rem;
      border-top: 2px solid var(--border-primary);
      text-align: center;
    }

    .modal-total strong {
      font-size: 1.125rem;
      color: var(--text-primary);
    }

    .modal-total span {
      color: var(--text-secondary);
      font-size: 0.9375rem;
    }

    /* Success/Error/Confirmation Modal Styles */
    .success-modal,
    .confirmation-modal {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 10000;
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s ease;
    }

    .success-modal.show,
    .confirmation-modal.show {
      opacity: 1;
      visibility: visible;
    }

    .modal-content-wrapper {
      background: white;
      border-radius: 16px;
      padding: 2.5rem;
      max-width: 480px;
      width: 90%;
      max-height: 90vh;
      overflow-y: auto;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      transform: scale(0.8);
      transition: transform 0.3s ease;
      text-align: center;
    }

    .success-modal.show .modal-content-wrapper,
    .confirmation-modal.show .modal-content-wrapper {
      transform: scale(1);
    }

    .modal-icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.5rem;
      animation: successPulse 0.6s ease-out;
    }

    .modal-icon.error {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
    }

    .modal-icon.warning {
      background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    }

    .modal-icon i {
      color: white;
      font-size: 2.5rem;
    }

    @keyframes successPulse {
      0% {
        transform: scale(0);
        opacity: 0;
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    .modal-title {
      font-size: 1.75rem;
      font-weight: 700;
      color: #111827;
      margin-bottom: 0.75rem;
    }

    .modal-message {
      color: #6b7280;
      font-size: 1rem;
      margin-bottom: 2rem;
      line-height: 1.6;
    }

    .modal-actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
    }

    .modal-btn {
      padding: 0.75rem 2rem;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      border: none;
      font-size: 0.9375rem;
      transition: all 0.2s;
      text-decoration: none;
      display: inline-block;
    }

    .modal-btn-primary {
      background: linear-gradient(135deg, #c51d34 0%, #a01628 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(197, 29, 52, 0.3);
    }

    .modal-btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(197, 29, 52, 0.4);
    }

    .modal-btn-secondary {
      background: #f3f4f6;
      color: #374151;
    }

    .modal-btn-secondary:hover {
      background: #e5e7eb;
    }

  </style>
</head>
<body>
  <div class="dashboard-layout">
    <%- include('../partials/admin-sidebar') %>
    
    <div class="dashboard-main">
      <div class="page-header">
        <div>
          <h1>Polls</h1>
          <p>Manage polls</p>
        </div>
        <div style="display: flex; gap: 0.75rem;">
          <a href="/admin/activities/polls/add" class="btn-primary" style="background: #10b981;">
            <i class="fas fa-plus"></i> Add Poll
          </a>
        </div>
      </div>

      <div class="table-container">
        <% if (polls && polls.length > 0) { %>
          <!-- Desktop Table View -->
          <div class="table-wrapper">
          <table class="table">
            <thead>
              <tr>
                <th>Question</th>
                <th>Total Votes</th>
                <th>Status</th>
                <th>Created</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% polls.forEach(poll => { %>
                <tr>
                  <td style="overflow: hidden; text-overflow: ellipsis; white-space: nowrap; max-width: 300px;"><strong><%= poll.question %></strong></td>
                  <td><strong><%= poll.totalVotes || 0 %></strong></td>
                  <td>
                    <span class="status-badge <%= poll.is_active ? 'active' : 'inactive' %>">
                      <%= poll.is_active ? 'Active' : 'Inactive' %>
                    </span>
                  </td>
                  <td><%= new Date(poll.created_at).toLocaleDateString() %></td>
                  <td>
                    <div class="action-buttons">
                      <a href="/admin/activities/polls/results/<%= poll.id %>" class="btn btn-info btn-sm btn-icon-only" style="text-decoration: none; display: inline-flex;" title="View Results">
                        <i class="fas fa-chart-bar"></i>
                      </a>
                      <% if (poll.is_active) { %>
                        <a href="/polls/<%= poll.id %>" target="_blank" class="btn btn-primary btn-sm btn-icon-only" style="text-decoration: none; display: inline-flex; background: #3b82f6;" title="View Poll">
                          <i class="fas fa-external-link-alt"></i>
                        </a>
                      <% } %>
                      <button class="btn btn-success btn-sm btn-icon-only toggle-poll" data-poll-id="<%= poll.id %>" data-status="<%= poll.is_active %>" title="<%= poll.is_active ? 'Deactivate' : 'Activate' %> Poll">
                        <i class="fas fa-<%= poll.is_active ? 'toggle-on' : 'toggle-off' %>"></i>
                      </button>
                      <a href="/admin/activities/polls/edit/<%= poll.id %>" class="btn btn-secondary btn-sm btn-icon-only" style="text-decoration: none; display: inline-flex;" title="Edit Poll">
                        <i class="fas fa-edit"></i>
                      </a>
                      <button class="btn btn-danger btn-sm btn-icon-only delete-poll" data-poll-id="<%= poll.id %>" data-poll-question="<%= poll.question %>" title="Delete Poll">
                        <i class="fas fa-trash"></i>
                      </button>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
          </div>

          <!-- Mobile Card View -->
          <div class="mobile-card-view">
            <% polls.forEach(poll => { %>
              <div class="mobile-card">
                <div class="mobile-card-header">
                  <div class="mobile-card-title">
                    <h3><%= poll.question %></h3>
                    <p><%= new Date(poll.created_at).toLocaleDateString() %></p>
                  </div>
                  <span class="status-badge <%= poll.is_active ? 'active' : 'inactive' %>">
                    <%= poll.is_active ? 'Active' : 'Inactive' %>
                  </span>
                </div>
                <div class="mobile-card-body">
                  <div class="mobile-card-row">
                    <span class="mobile-card-label">Votes:</span>
                    <span class="mobile-card-value">
                      <div class="mobile-card-votes">
                        <i class="fas fa-chart-bar"></i>
                        <span><%= poll.totalVotes || 0 %></span>
                      </div>
                    </span>
                  </div>
                </div>
                <div class="mobile-card-actions">
                  <a href="/admin/activities/polls/results/<%= poll.id %>" class="btn btn-info" style="text-decoration: none;">
                    <i class="fas fa-chart-bar"></i> Results
                  </a>
                  <% if (poll.is_active) { %>
                    <a href="/polls/<%= poll.id %>" target="_blank" class="btn btn-primary" style="text-decoration: none; background: #3b82f6;">
                      <i class="fas fa-external-link-alt"></i> View
                    </a>
                  <% } %>
                  <button class="btn btn-success toggle-poll" data-poll-id="<%= poll.id %>" data-status="<%= poll.is_active %>">
                    <i class="fas fa-<%= poll.is_active ? 'toggle-on' : 'toggle-off' %>"></i> <%= poll.is_active ? 'Hide' : 'Show' %>
                  </button>
                  <a href="/admin/activities/polls/edit/<%= poll.id %>" class="btn btn-secondary" style="text-decoration: none;">
                    <i class="fas fa-edit"></i> Edit
                  </a>
                  <button class="btn btn-danger delete-poll" data-poll-id="<%= poll.id %>" data-poll-question="<%= poll.question %>">
                    <i class="fas fa-trash"></i>
                  </button>
                </div>
              </div>
            <% }) %>
          </div>
        <% } else { %>
          <div class="empty-state">
            <i class="fas fa-poll"></i>
            <h3>No Polls Found</h3>
          </div>
        <% } %>
      </div>

      <!-- Pagination -->
      <% if (pagination && pagination.totalPages > 1) { %>
        <div class="pagination-container">
          <div class="pagination-info">
            Showing <%= ((pagination.currentPage - 1) * pagination.limit) + 1 %> to <%= Math.min(pagination.currentPage * pagination.limit, pagination.totalCount) %> of <%= pagination.totalCount %> results
          </div>
          <div class="pagination">
            <% 
              const buildQuery = (page) => {
                return `?page=${page}`;
              };
            %>
            <% if (pagination.hasPrev) { %>
              <a href="/admin/activities/polls<%= buildQuery(pagination.currentPage - 1) %>" class="pagination-btn">
                <i class="fas fa-chevron-left"></i>
              </a>
            <% } else { %>
              <span class="pagination-btn disabled">
                <i class="fas fa-chevron-left"></i>
              </span>
            <% } %>

            <% 
              let startPage = Math.max(1, pagination.currentPage - 2);
              let endPage = Math.min(pagination.totalPages, pagination.currentPage + 2);
              if (endPage - startPage < 4) {
                if (startPage === 1) {
                  endPage = Math.min(5, pagination.totalPages);
                } else {
                  startPage = Math.max(1, pagination.totalPages - 4);
                }
              }
            %>

            <% if (startPage > 1) { %>
              <a href="/admin/activities/polls<%= buildQuery(1) %>" class="pagination-btn">1</a>
              <% if (startPage > 2) { %>
                <span class="pagination-ellipsis">...</span>
              <% } %>
            <% } %>

            <% for (let i = startPage; i <= endPage; i++) { %>
              <% if (i === pagination.currentPage) { %>
                <span class="pagination-btn active"><%= i %></span>
              <% } else { %>
                <a href="/admin/activities/polls<%= buildQuery(i) %>" class="pagination-btn"><%= i %></a>
              <% } %>
            <% } %>

            <% if (endPage < pagination.totalPages) { %>
              <% if (endPage < pagination.totalPages - 1) { %>
                <span class="pagination-ellipsis">...</span>
              <% } %>
              <a href="/admin/activities/polls<%= buildQuery(pagination.totalPages) %>" class="pagination-btn"><%= pagination.totalPages %></a>
            <% } %>

            <% if (pagination.hasNext) { %>
              <a href="/admin/activities/polls<%= buildQuery(pagination.currentPage + 1) %>" class="pagination-btn">
                <i class="fas fa-chevron-right"></i>
              </a>
            <% } else { %>
              <span class="pagination-btn disabled">
                <i class="fas fa-chevron-right"></i>
              </span>
            <% } %>
          </div>
        </div>
      <% } %>
    </div>
  </div>

  <!-- Results Modal -->
  <div class="modal-overlay" id="resultsModal">
    <div class="modal-content">
      <div class="modal-header">
        <h2 id="modalPollQuestion">Poll Results</h2>
        <button class="modal-close" id="closeModal">
          <i class="fas fa-times"></i>
        </button>
      </div>
      <div class="modal-results" id="modalResultsContent">
        <!-- Results will be populated here -->
      </div>
    </div>
  </div>

  <script>

    function showModal(modalId) {
      const modal = document.getElementById(modalId);
      if (modal) {
        modal.classList.add('show');
      }
    }

    function closeModal(modalId) {
      const modal = document.getElementById(modalId);
      if (modal) {
        modal.classList.remove('show');
      }
    }

    function showConfirmation(message, onConfirm) {
      const modal = document.getElementById('confirmationModal');
      const messageEl = document.getElementById('confirmationMessage');
      const confirmBtn = document.getElementById('confirmBtn');
      
      if (modal && messageEl) {
        messageEl.textContent = message;
        modal.classList.add('show');
        
        const newConfirmBtn = confirmBtn.cloneNode(true);
        confirmBtn.parentNode.replaceChild(newConfirmBtn, confirmBtn);
        
        newConfirmBtn.onclick = function() {
          closeModal('confirmationModal');
          if (onConfirm) onConfirm();
        };
      }
    }

    document.querySelectorAll('.toggle-poll').forEach(btn => {
      btn.addEventListener('click', async function() {
        const pollId = this.dataset.pollId;
        const currentStatus = this.dataset.status === 'true';

        try {
          const response = await fetch(`/admin/activities/polls/${pollId}/toggle-active`, {
            method: 'POST'
          });

          const data = await response.json();

          if (data.success) {
            location.reload();
          } else {
            document.getElementById('errorMessage').textContent = data.message || 'Error toggling poll status';
            showModal('errorModal');
          }
        } catch (error) {
          console.error('Error:', error);
          document.getElementById('errorMessage').textContent = 'An error occurred while toggling poll status';
          showModal('errorModal');
        }
      });
    });

    document.querySelectorAll('.delete-poll').forEach(btn => {
      btn.addEventListener('click', async function() {
        const pollId = this.dataset.pollId;
        const pollQuestion = this.dataset.pollQuestion;

        showConfirmation(`Are you sure you want to delete poll "${pollQuestion}"? This action cannot be undone.`, async function() {
          try {
            const response = await fetch(`/admin/activities/polls/${pollId}`, {
              method: 'DELETE'
            });

            const data = await response.json();

            if (data.success) {
              document.getElementById('successMessage').textContent = 'Poll deleted successfully';
              showModal('successModal');
              setTimeout(() => {
                location.reload();
              }, 1500);
            } else {
              document.getElementById('errorMessage').textContent = data.message || 'Error deleting poll';
              showModal('errorModal');
            }
          } catch (error) {
            console.error('Error:', error);
            document.getElementById('errorMessage').textContent = 'An error occurred while deleting the poll';
            showModal('errorModal');
          }
        });
      });
    });


    const resultsModal = document.getElementById('resultsModal');
    const closeResultsModalBtn = document.getElementById('closeModal');
    const modalPollQuestion = document.getElementById('modalPollQuestion');
    const modalResultsContent = document.getElementById('modalResultsContent');


    document.querySelectorAll('.view-results').forEach(btn => {
      btn.addEventListener('click', function() {
        const question = this.dataset.pollQuestion;
        const options = JSON.parse(this.dataset.pollOptions || '[]');
        const votes = JSON.parse(this.dataset.pollVotes || '[]');
        const totalVotes = parseInt(this.dataset.pollTotal || '0');

        modalPollQuestion.textContent = question;
        
        let resultsHTML = '<div class="poll-results">';
        
        if (options.length > 0 && totalVotes > 0) {
          options.forEach((option, index) => {
            const voteCount = votes[index] || 0;
            const percentage = totalVotes > 0 ? ((voteCount / totalVotes) * 100).toFixed(1) : 0;
            
            resultsHTML += `
              <div class="poll-result-item">
                <div class="poll-result-header">
                  <span class="poll-result-label">${option}</span>
                  <span class="poll-result-stats">${voteCount} (${percentage}%)</span>
                </div>
                <div class="poll-result-bar">
                  <div class="poll-result-fill ${voteCount === 0 ? 'no-votes' : ''}" style="width: ${percentage}%"></div>
                </div>
              </div>
            `;
          });
        } else if (options.length > 0) {
          resultsHTML += '<div style="color: var(--text-secondary); font-size: 0.9375rem; text-align: center; padding: 2rem;">No votes yet</div>';
        } else {
          resultsHTML += '<div style="color: var(--text-secondary); font-size: 0.9375rem; text-align: center; padding: 2rem;">No options available</div>';
        }
        
        resultsHTML += '</div>';
        
        if (totalVotes > 0) {
          resultsHTML += `
            <div class="modal-total">
              <strong>Total Votes: ${totalVotes}</strong>
            </div>
          `;
        }
        
        modalResultsContent.innerHTML = resultsHTML;
        resultsModal.classList.add('active');
      });
    });


    closeResultsModalBtn.addEventListener('click', () => {
      resultsModal.classList.remove('active');
    });


    resultsModal.addEventListener('click', (e) => {
      if (e.target === resultsModal) {
        resultsModal.classList.remove('active');
      }
    });


    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && resultsModal.classList.contains('active')) {
        resultsModal.classList.remove('active');
      }
    });

    document.addEventListener('click', function(e) {
      if (e.target.classList.contains('success-modal') || e.target.classList.contains('confirmation-modal')) {
        closeModal(e.target.id);
      }
    });
  </script>

  <!-- Success Modal -->
  <div class="success-modal" id="successModal">
    <div class="modal-content-wrapper">
      <div class="modal-icon">
        <i class="fas fa-check"></i>
      </div>
      <h3 class="modal-title">Success!</h3>
      <p class="modal-message" id="successMessage">Operation completed successfully.</p>
      <div class="modal-actions">
        <button onclick="closeModal('successModal')" class="modal-btn modal-btn-primary">OK</button>
      </div>
    </div>
  </div>

  <!-- Error Modal -->
  <div class="success-modal" id="errorModal">
    <div class="modal-content-wrapper">
      <div class="modal-icon error">
        <i class="fas fa-exclamation-triangle"></i>
      </div>
      <h3 class="modal-title">Error Occurred</h3>
      <p class="modal-message" id="errorMessage">Something went wrong. Please try again.</p>
      <div class="modal-actions">
        <button onclick="closeModal('errorModal')" class="modal-btn modal-btn-primary">Close</button>
      </div>
    </div>
  </div>

  <!-- Confirmation Modal -->
  <div class="confirmation-modal" id="confirmationModal">
    <div class="modal-content-wrapper">
      <div class="modal-icon warning">
        <i class="fas fa-question-circle"></i>
      </div>
      <h3 class="modal-title">Confirm Action</h3>
      <p class="modal-message" id="confirmationMessage">Are you sure you want to proceed?</p>
      <div class="modal-actions">
        <button onclick="closeModal('confirmationModal')" class="modal-btn modal-btn-secondary">Cancel</button>
        <button id="confirmBtn" class="modal-btn modal-btn-primary">Confirm</button>
      </div>
    </div>
  </div>
</body>
</html>

